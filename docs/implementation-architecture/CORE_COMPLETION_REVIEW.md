# Aurora — Core Completion Review

> A consolidation of what Aurora's reasoning core has proven in code, what it deliberately does not yet do, and the boundaries future work must not collapse.
>
> Review / Consolidation phase. No code, no production-module changes. This document summarizes; it does not decide.

| Field | Value |
|---|---|
| **Status** | Review / Consolidation · *Accepted snapshot* |
| **Phase** | Review (no implementation) |
| **Covers** | Implementations 001–006 (core) · **updated for 007 (Purpose `athlete`), 008 (projection freshness), 009 (AthleteDecision feedback), 010 (persistence ports + in-memory repositories), 011 (event/outcome records + traceability envelope), 012 (check-only reprojection harness), 013 (manual input adapter), 014 (deterministic rendering boundary), 015 (rendered-message record/review persistence), 016 (delivery boundary), 017 (provider adapter seam), 018 (provider-attempt audit), 019 (real-provider-ready boundary), 020 (concrete-provider adapter shell), 021 (opt-in live-provider boundary)** |
| **Validation at writing** | core: `145/145` · 007: `175/175` · 008: `196/196` · 009: `212/212` · 010: `235/235` · 011: `275/275` · 012: `295/295` · 013: `317/317` · 014: `349/349` · 015: `374/374` · 016: `397/397` · 017: `421/421` · 018: `441/441` · 019: `462/462` · 020: `493/493` · **post-021: `tsc --noEmit` clean · `node --test` 514/514 pass** |
| **Modules** | `observation` (intake **+ Manual Input Adapter**, Impl 013), `reasoning`, `understanding` (projection freshness), `decision-support`, **`athlete` (Purpose + AthleteDecision)** (+ `shared-kernel`) — each with **repository ports + in-memory adapters** (Impl 010) — **plus the dependency-neutral `event-recording` module** (append-only, ref-only `DomainEventRecord` log) (Impl 011) — **plus a neutral check-only `reprojection-harness`** (test-support seam, not a module) (Impl 012) — **plus the downstream `rendering` module** (deterministic fake renderer + mandatory validator (Impl 014) **+ rendered-message record/review repository (Impl 015) + provider adapter seam with a deterministic fake provider (Impl 017) + provider-attempt audit repository (Impl 018) + real-provider-ready async client boundary (Impl 019) + selected-provider adapter shell, neutral `concrete-provider-*`, disabled by default (Impl 020) + opt-in live-provider boundary, fail-closed, native `fetch` isolated in one approved transport file (Impl 021)**) — **plus the downstream `delivery` module** (display-eligibility-gated exposure to a deterministic test sink + audit records (Impl 016)) |

[FACT] **Central question:** *What exactly has Aurora's reasoning core proven in code, what does it intentionally not do yet, and what boundaries must future work not violate?*

---

## Update — Implementation 007 (Purpose-first `athlete`)

[FACT] Since this review was first written for Implementations 001–006, **Implementation 007** added a thin, **Purpose-first `athlete` module** — Aurora's first real *given* context. The body below has been corrected where it is affected; this banner is the at-a-glance summary of *what changed after Implementation 007*.

[FACT] **Now true:**
- `athlete` is an **implemented upstream module** (imports only `shared-kernel`; imports no `observation`/`reasoning`/`understanding`/`decision-support`).
- The implemented slice is **Purpose-only**: `Athlete` (thin root), `Purpose`/`DeclaredPurpose`, `PurposeVersion`, `PurposeVersionRef`, `PurposeHistory`, `PurposeChanged`, `PurposeChangeReason`, `PurposeSource`, `PurposeStatus`, `PurposeReinterpretationStatus` (type only), and a `RevealedPurposeSignal` placeholder.
- **`PurposeHistory` is append-only**; a change appends an immutable `PurposeVersion` and never overwrites the past.
- **`PurposeChanged` is a domain outcome/value** (returned/derived), **not** an event bus.
- **`PurposeVersionRef` can flow into `Hypothesis.purposeContextRef`** (the slot already existed — no reasoning refactor).
- **Purpose maps to decision-support `PurposeContext`** (missing→unknown, ambiguous→ambiguous, declared→declared) through a **neutral harness adapter**.
- **A purpose change can selectively stale understanding** via `markUnderstandingStale("purpose-change")` through a neutral harness adapter — `athlete` never mutates `UnderstandingProfile` directly and never resets it globally.
- **`Athlete` owns declared context, not inferred truth** — no state, capacity, readiness, fatigue, constraints, or path-dependent memory were implemented.

[FACT] **Distinctions this update makes explicit** (do not collapse):
- **thin Athlete/Purpose module ≠ full Athlete aggregate** — only the Purpose slice exists.
- **declared Purpose ≠ inferred athlete state** — `athlete` holds the *given*, never the *inferred*.
- **`PurposeChanged` ≠ reasoning rewrite** — prior hypotheses are never edited or auto-falsified.
- **`PurposeVersionRef` ≠ proof that old reasoning used the new purpose** — it is a context handle, tagging which purpose was in force; it does not retroactively re-evaluate.
- **Revealed behavior ≠ declared purpose** — only an athlete-sourced declaration creates a version; behavior never silently overwrites it.
- **Purpose context ≠ decision-support voice** — purpose feeds the `PurposeGate`; the case still selects the `VoiceMode`.
- **selective staleness ≠ global understanding reset** — only the named dimension(s) go stale.

[ASSUMPTION] The headline: **Aurora now has athlete-owned, versioned Purpose as real upstream context — but not a full Athlete model.** Purpose can constrain future reasoning and decision-support through explicit seams, while prior reasoning stays historically traceable and is never rewritten.

---

## Update — Implementation 008 (Projection Freshness on `UnderstandingAssessment`)

[FACT] **Implementation 008** made projection freshness **explicit** on `UnderstandingAssessment` (the one concrete projection today) and added a pure, selective refresh policy — all inside `understanding`, with **no `decision-support` change**.

[FACT] **Now true:**
- `UnderstandingAssessment` carries explicit **`freshness`** (`current`/`stale`/`partial`/`invalid`/`unknown`), **`derivedAt`**, **`sourceRefs`** (a `ProjectionTrace`), and **`limitations`** — all additive/optional, so 001–007 call sites are unchanged.
- New `understanding` surface: `ProjectionFreshness`/`Status`, `StalenessReason`, `ProjectionSourceRef`/`Kind`, `ProjectionTrace`, `ProjectionLimitations`, `RefreshTrigger`/`Kind`, `ProjectionRefreshDecision`/`Kind`, `ProjectionRefreshPolicy`, plus `clampCeilingByFreshness`/`applyFreshness`.
- **`current` preserves the Impl 006 Reflection scenario** byte-for-byte.
- **No non-current freshness can raise the voice;** `stale`/`partial` lower the ceiling one step; **`invalid`/`unknown` clamp `safeVoiceCeiling` to `none`** (→ `Withholding`).
- **`decision-support` was not modified** — freshness reaches it **only through the existing `safeVoiceCeiling`**; `understandingGate` still reads only the ceiling.
- **Refresh = recompute** (a new assessment); `applyFreshness` produces a **new** view and never mutates the old one; the prior assessment stays auditable if retained.
- The refresh policy is **pure, deterministic, selective** (only projections whose source refs intersect the trigger are affected), and **conservative** under uncertainty — it never globally invalidates and never invents traceability.
- **Still absent (intentional):** no generic projection engine, no top-level `projection` module, no persistence, no DB, no cache, no event bus, no UI/API/LLM, no `ImpactAssessment`.

[FACT] **Distinctions this update makes explicit** (do not collapse):
- **Projection ≠ source of truth** — `UnderstandingAssessment` is a derived read model of the `UnderstandingProfile` aggregate.
- **`ProjectionFreshness` ≠ traceability** — freshness says *how safe to consume*; trace/source refs say *what it came from*.
- **source refs ≠ copied source state** — references back to real artifacts, never embedded/re-authored truth.
- **refresh ≠ mutate old projection** — refresh recomputes a new view; the old one is never edited.
- **stale/partial/invalid/unknown ≠ permission to recommend** — non-current freshness can only constrain.
- **`safeVoiceCeiling` clamp ≠ decision-support owning freshness** — the consumer reads the (clamped) ceiling; it does not read freshness.
- **`UnderstandingAssessment` projection ≠ `UnderstandingProfile` aggregate** — the aggregate is the source of truth; the assessment is its labeled view.
- **local freshness slice ≠ generic projection engine** — freshness lives in `understanding` for one projection; no engine exists.

[ASSUMPTION] The headline: **Aurora now makes projection freshness explicit for `UnderstandingAssessment`; non-current freshness can only constrain downstream voice through `safeVoiceCeiling`, projections stay derived views, and there is still no generic projection engine or persistence.**

---

## Update — Implementation 009 (AthleteDecision Feedback Loop)

[FACT] **Implementation 009** closed the decision feedback loop: an athlete-owned, append-only `AthleteDecision` slice was added **inside `athlete`**, and a neutral harness adapter re-enters a reported decision as a `SubjectiveObservation` — **no `decision-support` / `observation` / `reasoning` / `understanding` change**.

[FACT] **Now true:**
- `AthleteDecision` is **implemented inside `athlete`** (athlete-owned, append-only); the `AthleteDecisionRef` placeholder is **retired** (it now references a real decision).
- New `athlete` surface: `AthleteDecisionId`, `AthleteDecision` (+ athlete-local `AthleteDecisionRef`, `DecisionReportSource`), `DecisionChoice`, `DecisionRationale`, `DecisionContext`, `DecisionOutcomeRef`, `AthleteDecisionRecord` (+ `amend`/supersede). Coordinators: `recordAthleteDecision`, `amendAthleteDecision`.
- **`DecisionSupportCase` records only an `AthleteDecisionRef`** — it references the decision, never owns or mutates it.
- The decision is **append-only**: corrections **amend/supersede**, the original stays auditable; `DecisionChoice` keeps a **modification** as free text (no binary compliance); `DecisionRationale` is athlete-reported context, not truth.
- **`DecisionOutcomeRef` is a reference only** — no full `DecisionOutcome` object; "no outcome" is the default.
- The decision **re-enters as a `SubjectiveObservation`** (via the neutral `__tests__` adapter, since `athlete` must not import `observation`) — an `Observation`, **never** `Signal`/`Evidence`/`Hypothesis`/`Understanding`. Future learning still passes the full ladder.
- **`divergedFromSupport` is neutral factual metadata**, not a compliance score; **good/bad outcome does not grade `SupportQuality`**.
- **Still absent (intentional):** no full `DecisionOutcome` object, no pattern engine, no compliance/obedience/shame/reward scoring, no outcome-based validation, no UI/API/DB/event-bus/persistence/LLM/training-plan.

[FACT] **Distinctions this update makes explicit** (do not collapse):
- **`AthleteDecision` ≠ Aurora output** — it is the athlete's fact, not Aurora's product.
- **`AthleteDecisionRef` ≠ ownership** — a reference recorded after the fact.
- **divergence ≠ noncompliance**, **following ≠ obedience-success**, **not-following ≠ failure** — no valence anywhere.
- **`DecisionOutcomeRef` ≠ outcome judgement** — a handle to a separate, later observation.
- **`AthleteDecision → Observation` ≠ `AthleteDecision → Evidence`** — re-entry is observation only.
- **`SupportQuality` ≠ outcome quality** — integrity at the time of support.
- **decision pattern ≠ athlete label** — a pattern must become a falsifiable hypothesis first.
- **decision rationale ≠ declared-purpose overwrite** — behavior never rewrites purpose.

[ASSUMPTION] The headline: **the athlete's decision now returns as athlete-owned learning material — referenced not owned, re-entering only as `Observation` through the ladder — with no obedience tracking, no shame, and no outcome-based grading of the support.**

---

## Update — Implementation 010 (Persistence Ports & In-Memory Repositories)

[FACT] **Implementation 010** added the first persistence slice: **repository ports + in-memory adapters** and an additive, validated **`toState()` / `reconstitute(state)`** surface per persisted boundary — proving round-trip safety with **no DB, ORM, schema, migrations, event bus, cache, or infrastructure**, and **no technology chosen**. All domain changes are **additive** (no behavior change; the prior tests stayed green).

[FACT] **Now true:**
- Persisted boundaries gained `toState()` (plain, serializable) + `static reconstitute(state)` (**validates invariants; rejects invalid state; rebuilds via existing constructors/helpers** — never a raw field bag): `ObservationSet`, `Hypothesis`, `UnderstandingProfile`, `DecisionSupportCase`, `Athlete`, `AthleteDecisionRecord`.
- Each has a **module-owned repository port** (`save`/`findById`/`exists`) and an **in-memory adapter** (module-local `application/`, **not** infrastructure). `AthleteDecisionRecord` is keyed by `athleteRef`; **`PurposeHistory` persists *through* `Athlete`**.
- In-memory repositories **store state copies, not live references** (deep-copy via `structuredClone` on save *and* load), so **two finds return independent objects** and **mutating a retrieved object never touches the store** (mutation isolation proven).
- **Round-trip preserves**: identity, provenance, **append-only history**, **supersession**, **traceability refs**, terminal output + support integrity, and **`AthleteDecisionRef` (referenced, not owned)**.
- **Projection freshness survival** is proven via an **in-memory test helper** — there is **no projection repository**; a stored assessment loads still as a labeled view (invalid/unknown → ceiling `none`).
- **Still absent (intentional):** no production DB/ORM/schema/migrations, no event bus, no cache, no `src/infrastructure`, no `persistence`/`repositories` module, no projection repository, no event records.

[FACT] **Distinctions this update makes explicit** (do not collapse):
- **persistence ports ≠ production database** · **in-memory repository ≠ infrastructure layer**.
- **`toState()` ≠ domain authority** · **`reconstitute(state)` ≠ a raw field-bag bypass** (it validates).
- **persisted state ≠ current truth** · **repository round-trip ≠ event sourcing**.
- **traceability refs ≠ database foreign keys** · **projection-freshness survival helper ≠ projection repository**.
- **`DecisionSupportCase` repository ≠ `AthleteDecision` ownership** · **a state copy ≠ a live domain-object reference**.

[ASSUMPTION] The headline: **Aurora can now save and reload its aggregates and stateful records through validated ports + in-memory adapters without corrupting invariants, traceability, freshness, append-only history, or ownership — while production persistence, event records, and all infrastructure remain intentionally absent.**

---

## Update — Implementation 011 (Domain Event/Outcome Records & Traceability Envelope)

[FACT] **Implementation 011** added a new **dependency-neutral `event-recording` module** that records *what happened* as an **append-only, ref-only** log — turning the persistence paper's §4 event surface from conceptual into code, while staying **non-command, non-bus, non-infrastructure**. It is **additive**: the only existing-file change is a documented one-line test allowlist for the new module directory; **no production module was modified**.

[FACT] **Now true:**
- A new module `src/modules/event-recording/` exists and **imports only `shared-kernel`**; **no domain module imports it**; the **event catalog lives outside `shared-kernel`** (the kernel never becomes event-aware).
- **One `DomainEventRecord`** carries two categories — **`occurrence`** (something was observed/created) and **`outcome`** (the result of an operation) — over a **closed 26-type catalog** (`DOMAIN_EVENT_TYPE`); there is **no parallel `DomainOutcomeRecord` class**.
- A record carries: id, type, category, `occurredAt`, `recordedAt`, producing module, a **`TraceabilityEnvelope`** (primary artifact ref + source refs + typed slots + optional projection-freshness *status label* + limitations), **ref-only `EventPayloadRef[]`**, and optional `EventActor` / `CausationRef` / `CorrelationRef`.
- **Payloads are ref-only by construction** — `EventPayloadRef` carries only `kind`/`id`/`role?`/`ownerModule?`; **copied aggregate state and arbitrary `metadata`/`payload` bags are unrepresentable**, and reconstitution rejects smuggled extra keys.
- **Records are append-only** — `DomainEventRecordLog` is immutable-by-operation (`append` returns a new log; duplicate ids rejected; order preserved); corrections/supersessions are **new records**. The **in-memory repository stores copies, not live references** (deep-copy on append/find/all) and exposes **no** `update`/`delete`/`dispatch`/`publish`/`subscribe`/`markProcessed`.
- **Catalog-driven validation** runs on both construction *and* `reconstitute(state)`: type↔module, type↔category, required primary-ref kind, required refs, required freshness marker, required output-kind role, required athlete actor, and `occurredAt ≤ recordedAt`. Reconstitution **re-validates and rejects invalid state**, executes nothing, and changes no timestamp.
- **Causation = lineage, correlation = grouping** — neither forms an execution chain; appending a record triggers no downstream mutation.
- **Still absent (intentional):** no event bus, no publish/subscribe, no handlers, no async delivery, no DB/schema/serialization tech, no `src/infrastructure`, no event sourcing, no projection engine, no production event store.

[FACT] **Distinctions this update makes explicit** (do not collapse):
- **event record ≠ command** · **event record ≠ aggregate** · **event record ≠ projection** · **event record ≠ source truth**.
- **event record ≠ event-bus message** · **event log ≠ event sourcing** (it records occurrences; it does not rebuild aggregates — `reconstitute` does).
- **correlation ≠ command chain** · **causation ≠ handler trigger**.
- **payload ref ≠ copied state** · **traceability envelope ≠ database foreign key**.
- **`DomainEventRecordRepository` ≠ production event store** · **occurrence/outcome record ≠ downstream mutation**.
- **`TerminalOutputSelected` event ≠ `AthleteDecision`** · **`AthleteDecisionRecorded` event ≠ compliance score** · **`ProjectionFreshnessChanged` event ≠ projection made current by assertion**.

[ASSUMPTION] The headline: **Aurora now records *what happened* in an append-only, ref-only, traceable log — complementing the aggregate repositories (which answer *what is the aggregate now?*) without replacing them, and without becoming a command, copied state, a projection, source truth, a bus, or event sourcing.**

---

## Update — Implementation 012 (Check-Only Reprojection Harness)

[FACT] **Implementation 012** added a **neutral, check-only reprojection harness** — a recompute-and-compare *safety mechanism* that re-derives derived views, recalculates freshness, verifies traceability, detects candidates from event records, and **reports** drift/findings. It is **test-support / coordination seam, not production architecture**: it lives under `src/modules/__tests__/reprojection-harness/`, **no production `reprojection` module exists**, and **no existing module was modified**.

[FACT] **Now true:**
- A check-only harness (`runReprojection`) exists under `src/modules/__tests__/reprojection-harness/` — composed of `ReprojectionRun`/`Result`/`Finding`/`Mode`/`Target`/`InputSet`, a pure event→candidate map, and an `understanding` recompute adapter.
- It **recomputes `UnderstandingAssessment`** *through the owning module's existing function* (`produceUnderstandingAssessment` + `applyFreshness` + the Impl 008 freshness helpers) — the harness **coordinates, it does not reason**; it never derives a level or applies a transition.
- It **recalculates the 5-state freshness** (and may only re-derive the **same or a more cautious** view — never promotes freshness, never raises a ceiling).
- It **detects candidate targets from `DomainEventRecord`s** via a **pure** map (`PurposeChanged`→stale, `ObservationSuperseded`→invalid/source-superseded, hypothesis events→`requires-policy-transition`, etc.) — events **identify what to check**, they **execute nothing**.
- It **reports drift** (`changed` + a differences summary) instead of overwriting; **reports** `missing-traceability`/`stale`/`invalid`/`requires-policy-transition`/`manual-review-required`; and attaches a diagnostic `SafeActionCategory` (never athlete-facing).
- **`check-only` is the default and only implemented mode**; `refresh-derived`/`mark-stale` are **reserved and throw**. A run **mutates no repository, appends no event record, creates no decision-support output, and calls no write method** (proven by a before/after deep-equality snapshot and a throwing-`save` that is never reached).
- **Empty repositories are never rebuilt from the event log** — an event referencing an absent aggregate yields `event-record-only`/`missing-source`, never synthesized state.
- **Still absent (intentional):** no production `reprojection` module, no scheduler, no event bus, no event sourcing, no projection repository, no production service/orchestration layer.

[FACT] **Distinctions this update makes explicit** (do not collapse):
- **reprojection harness ≠ production service** · **check-only ≠ a write path** · **reprojection ≠ event sourcing**.
- **event records ≠ aggregate-rebuild commands** · **events-as-candidates ≠ event execution** · **repository state ≠ event log**.
- **recomputed projection ≠ source truth** · **drift report ≠ overwrite** · **freshness recalculation ≠ freshness promotion**.
- **stale/invalid finding ≠ recommendation** · **DecisionSupport review finding ≠ `TerminalOutput`** · **`AthleteDecision` outcome ≠ `SupportQuality` rewrite** · **purpose-related stale finding ≠ `Purpose` overwrite** · **reserved mode ≠ implemented mode**.

[ASSUMPTION] The headline: **Aurora can now recompute and verify its derived views — recalculating freshness, identifying candidates from occurrence history, and reporting drift — while provably mutating no aggregate, executing no event, rebuilding nothing from the log, and turning no projection into truth; and it does so as a neutral check-only harness, not production architecture.**

---

## Update — Implementation 013 (Manual Input Adapter)

[FACT] **Implementation 013** added Aurora's first **real "data in" boundary**: an **`observation`-owned Manual Input Adapter** that records manually supplied athlete/training information faithfully as an `ObservationSet` — and refuses what it cannot represent. It records *source material*, never *meaning*. It is **additive**: `observation/index.ts` gained exports only (no behavior change); the one other change is a documented test-only guard-precision fix.

[FACT] **Now true:**
- `src/modules/observation/application/manual-input-*` implements `ManualInputSubmission`, `ManualInputEntry` (closed union), `ManualInputIngestionOutcome` (`accepted`/`partially-accepted`/`rejected`), closed **rejection/limitation/quality** catalogs, and **`ingestManualInput({ submission, observationSetRepository })`**.
- The adapter's **only domain output is an `ObservationSet`** — built via the existing **`recordObservationSet`** and persisted **only** through **`ObservationSetRepository`** (no bypass, no DB).
- It preserves **verbatim subjective `words`**, records **explicit missing data** (never invents values), and preserves **provenance/quality** with **`source: "manual"`** (the reporter is preserved in the provenance reference).
- **Rejection saves nothing** (a throwing `save` is never reached); **partial acceptance** records only the faithful entries and reports limitations for the rest; ambiguity becomes a limitation or a rejection, never an inference.
- `measured-value` is **reserved** (no unit interpretation yet → limitation/rejection); `athlete-decision-report` is recorded **only as a subjective/context observation** — **no `AthleteDecisionRecord` mutation**, no compliance score.
- The adapter **imports no downstream module** (`reasoning`/`understanding`/`decision-support`/`athlete`) and **no `event-recording`**; **`observation` stays `event-recording`-free**. An optional `ObservationSetRecorded` is composed **only in a neutral harness** from a **ref-only** event candidate, and is inert.
- **Still absent (intentional):** no UI, API, LLM, external/FIT/wearable integration, DB/schema, event bus, scheduler.

[FACT] **Distinctions this update makes explicit** (do not collapse):
- **manual input ≠ meaning** · **adapter ≠ reasoning** · **saved `ObservationSet` ≠ `Signal` detection**.
- **subjective words ≠ inferred fatigue/readiness** · **missing data ≠ invented value** · **partial acceptance ≠ silent interpretation** · **rejection ≠ accidental data loss**.
- **source quality ≠ athlete quality** · **event candidate ≠ event command** · **`ObservationSetRecorded` ≠ downstream execution**.
- **athlete-decision report as observation ≠ `AthleteDecisionRecord` mutation** · **Manual Input Adapter ≠ UI/API/LLM/external integration**.

[ASSUMPTION] The headline: **Aurora now accepts manually supplied input as faithful `ObservationSet`s — preserving provenance/source/quality and verbatim words, representing missing data explicitly, rejecting the unrepresentable, and persisting through the repository port — without interpreting input, detecting signals, reasoning, mutating athlete records, or triggering any downstream effect; the adapter is a scribe, not an interpreter.**

---

## Update — Implementation 014 (Deterministic LLM Rendering Boundary)

[FACT] **Implementation 014** added Aurora's first **"output out"** boundary: a downstream **`rendering` module** that turns a domain-approved `TerminalOutput` into human-facing text — preserving the domain's voice, uncertainty, limitations, freshness, traceability, and agency. The renderer **expresses**; it never **decides, escalates voice, strengthens a claim, hides uncertainty, invents a fact, or becomes authority**. It is **additive**: the only existing-file change is the documented `ALLOWED_MODULES` allowlist update for the new module.

[FACT] **Now true:**
- A new module `src/modules/rendering/` exists: `RenderableDomainOutput` (+ module-local `RenderableFreshness`), `RenderingRequest` (+ closed `SAFE_STYLES`/`SUPPORTED_LOCALES`), `RenderedMessage`/`RenderOutcome`, a closed **12-value `RenderingFailure`** catalog, the `RenderingPolicy` cue vocabularies, a **mandatory `RenderingValidator`**, a deterministic **fake renderer**, and `renderableFromTerminalOutput`.
- **Rendering is downstream presentation, not reasoning and not `decision-support`.** `decision-support` owns the `TerminalOutput` and `VoiceMode`; the renderer reads a **read-only projection** and owns **phrasing only**.
- The **fake renderer is deterministic** (no provider, no model, no randomness; same request → same text); **every draft passes the mandatory validator** before becoming a `RenderedMessage`. **The validator, not the renderer, is the safety guarantee.**
- The validator (structural/string-based) **rejects**: voice escalation, recommendation language where not domain-approved, inquiry-rendered-as-answer, withholding-rendered-as-advice, hidden uncertainty, hidden limitations, missing/invented traceability (completeness claims over an incomplete chain), invented facts, unsupported style/locale, and unsafe athlete-state/purpose/compliance/event-command language — and any failure **degrades to safe non-rendering**.
- **Voice may match or soften, never escalate**; `Inquiry` stays a question; `Withholding` stays a refusal; `Recommendation` preserves conditions, uncertainty, traceability, and agency.
- A `RenderedMessage` is **not domain authority** — not `Evidence`/`Observation`/`Understanding`/`AthleteDecision`, not source truth; rendering **mutates no aggregate**, **writes no event**, and **persists nothing**.
- `rendering` **imports only `shared-kernel` + read-only `decision-support` types**; **no existing domain module imports `rendering`**; **no real LLM provider / prompt templates / UI / API / external call** exists.
- **Still absent (intentional):** a real LLM provider, prompt templates as production code, UI/API/external delivery, rendered-message persistence, and rendered-output event records.

[FACT] **Distinctions this update makes explicit** (do not collapse):
- **rendering ≠ reasoning** · **generated text ≠ domain authority** · **renderer ≠ voice selector** · **`VoiceMode` ≠ style request**.
- **fake renderer ≠ LLM provider** · **validator ≠ model quality** · **renderable output ≠ raw reasoning internals**.
- **rendered message ≠ `Evidence`/`Observation`/`Understanding`/`AthleteDecision`** · **rendering failure ≠ unsafe fallback**.
- **style request ≠ permission to escalate voice** · **Recommendation rendering ≠ recommendation creation** · **Withholding rendering ≠ advice** · **Inquiry rendering ≠ answer** · **traceability summary ≠ invented citation**.
- **rendered-text persistence ≠ implemented** · **event record for a rendered message ≠ implemented**.

[ASSUMPTION] The headline: **Aurora can now express a domain-approved terminal output in human-facing language — deterministically, behind a mandatory validator — while the generated text never selects voice, strengthens a claim, hides uncertainty, invents a fact, mutates state, or becomes authority; rendering expresses the domain's decision, it never makes one.**

---

## Update — Implementation 015 (Rendered-Message Record / Review Persistence)

[FACT] **Implementation 015** closed the first **auditable output-out cycle**: a **rendered-message record + display-safety review**, kept **inside `rendering`** (not a new module). It records and reviews presentation artifacts **without** turning generated text into domain truth or review approval into reasoning. It is **additive**: only the two `rendering` index files changed (additive exports); **no new top-level module**, **no documented blocker**.

[FACT] **Now true:**
- `rendering` gained (in `domain/` + `application/`): `RenderedMessageRecordId`/`RenderReviewId`, `RenderedMessageRecord` (+ `RenderedMessageRecordState`), `RenderReview` + closed `RenderReviewDecision` (5) / `RenderReviewReason` (11) / `ReviewerKind`, `DisplayEligibility` + `displayEligibilityOf`, and a `RenderedMessageRecordRepository` **port** + `InMemoryRenderedMessageRecordRepository`.
- A `RenderedMessageRecord` is an **append-only, auditable presentation artifact** that **preserves the source domain output ref**, terminal output kind, `VoiceMode` at render time, text (if rendered), validation/preservation flags, renderer kind, and `createdAt`. **It is not domain authority** (not `Evidence`/`Observation`/`Understanding`/`DecisionSupport`/`AthleteDecision`/source truth).
- **Review history is append-only**; **current review status is derived** from the latest entry (`not-reviewed` initially; `superseded` from `supersededBy`). **Display eligibility is derived** (rendered + `approved-for-display` + not superseded + source ref + preservation flags intact) — and **display eligibility is not delivery, not domain approval, not recommendation creation; it triggers nothing**.
- **Approval changes no domain** — not `VoiceMode`, confidence, traceability, freshness/limitations, or `SupportQuality`; **rejection invalidates nothing** upstream; **revision/supersession preserve** the old record (immutable; no silent overwrite); a **failed attempt** is auditable but **never display-eligible and cannot be approved**.
- The **repository** uses the Impl 010 pattern (deep-copy `toState()`↔`reconstitute()`; **mutation isolation**; **invalid state rejected**); **no production DB/schema/ORM/migration**.
- **No rendered-output events**: a `RenderedMessageRecord` is **not an event record**; **`rendering` imports no `event-recording`** and the **event catalog is not expanded**. **No delivery / UI / API / provider / prompt templates.**

[FACT] **Distinctions this update makes explicit** (do not collapse):
- **rendered-message persistence ≠ domain authority** · **review approval ≠ stronger evidence** · **display eligibility ≠ delivery** · **presentation review ≠ reasoning review**.
- **persisted rendered text ≠ `Observation`/`Evidence`/`Understanding`/`DecisionSupport`/`AthleteDecision`** · **source ref preservation ≠ source-truth conversion**.
- **review rejection ≠ domain invalidation** · **revision ≠ overwrite** · **supersession ≠ deletion** · **failed render audit ≠ displayable message**.
- **repository persistence ≠ production DB** · **rendered-message record ≠ event record** · **no rendered-output event ≠ missing auditability**.

[ASSUMPTION] The headline: **Aurora can now conserve and review rendered text as an auditable presentation artifact — append-only records, append-only review, derived display eligibility — improving auditability and display safety only, never making the text domain truth, mutating the domain, repairing traceability, emitting an event, or triggering delivery.**

---

## Update — Implementation 016 (Delivery Boundary)

[FACT] **Implementation 016** added the first **delivery / exposure** boundary: a **new downstream `delivery` module** that exposes a *display-eligible* `RenderedMessageRecord` to a target and records the attempt as an auditable `DeliveryRecord`. Delivery is **downstream exposure**, not rendering and not domain — it **verifies** eligibility, it never decides, interprets, or reinterprets. It is **additive**: two **documented test-only** blocker fixes landed (the e2e `ALLOWED_MODULES` set gained `delivery`; the Impl 015 forbidden-layer test dropped its obsolete `delivery` entry); **no production module was modified**.

[FACT] **Now true:**
- A new module `src/modules/delivery/` exists, importing **only `shared-kernel` + read-only `rendering`** types/functions; it imports **no** `observation`/`reasoning`/`understanding`/`decision-support`/`athlete`/`event-recording`; **`rendering` does not import `delivery`**; **no upstream module imports `delivery`**.
- `domain/` implements: ids (`DeliveryRequestId`/`DeliveryRecordId`), the closed **`DeliveryTarget`** (only `test-sink` supported; `manual-review-surface`/`future-ui`/`future-notification-channel` reserved), the closed **`DeliveryOutcome`** (`accepted-for-delivery` [reserved] / `blocked-not-eligible` / `delivered` / `failed` / `cancelled` / `not-attempted`), the closed **`DeliveryFailureReason`** (10 reasons; `provider-unavailable` **excluded**, `sink-unavailable` added), `DeliveryRequest`, `DeliveryEligibilityCheck` (+ the raw-reason→failure-reason mapping), the `DeliverySink` interface + deterministic **`InMemoryTestSink`**, and the auditable `DeliveryRecord` (+ `DeliveryRecordState`, validated `toState`/`reconstitute`).
- `application/` implements the **`DeliveryRecordRepository` port** + `InMemoryDeliveryRecordRepository` (deep-copy round-trip, mutation isolation, validated reconstitution, `findByRenderedMessageRecordRef`) + the **`requestDelivery`** service (the only file importing `rendering`).
- **Eligibility is reused, not reinterpreted:** `requestDelivery` calls `rendering`'s **`displayEligibilityOf(record)`** and maps its specific reasons to a specific `DeliveryFailureReason` (raw reasons retained). The deterministic sink is called **only** when the record is eligible *and* the target is the supported `test-sink`; **not-reviewed / rejected / superseded / failed-render / missing-ref** records and **unsupported/reserved targets** are **blocked** without calling the sink.
- **Delivery is auditability, not authority:** a `DeliveryRecord` is **not** source truth / `Evidence` / `Observation` / `Understanding` / `DecisionSupport` / `AthleteDecision`; **delivery success is not evidence** and **delivery failure is not domain invalidation**; delivery **mutates no rendered-message record and no aggregate**, **triggers no reasoning/reprojection/retry**, and **emits no event**.
- **Still absent (intentional):** no real provider/channel (email/SMS/push/WhatsApp/web), no UI/API, no scheduler/retry/background-job layer, no event bus, no **delivery event records**, no event-catalog expansion, no production DB/schema/ORM/migration.

[FACT] **Distinctions this update makes explicit** (do not collapse):
- **delivery ≠ rendering** · **delivery ≠ domain reasoning** · **display eligibility ≠ delivery**.
- **delivery verifies `displayEligibilityOf` ≠ delivery re-derives eligibility** — verify, never reinterpret.
- **`DeliveryRecord` ≠ `Evidence`/`Observation`/`Understanding`/`AthleteDecision`/`DecisionSupport`** · **delivery success ≠ support validation** · **delivery failure ≠ domain invalidation**.
- **`test-sink` ≠ real provider/channel** · **`InMemoryTestSink` ≠ email/SMS/push/WhatsApp/UI/API**.
- **delivery audit ≠ event record** · **delivery repository ≠ production DB** · **no delivery event ≠ missing auditability**.
- **accepted/delivered outcome ≠ athlete outcome** · **delivered text ≠ source truth** · **retry/scheduler ≠ implemented** · **`accepted-for-delivery` ≠ produced this slice (reserved)**.

[ASSUMPTION] The headline: **Aurora can now expose a reviewed, display-eligible rendered message to a deterministic test sink and record the attempt as an auditable `DeliveryRecord` — exposure and auditability only, never authority: it verifies (not re-derives) eligibility, blocks everything ineligible, and lets neither delivery success nor failure touch the domain, become evidence, or trigger reasoning; and it does so behind a closed boundary with no real channel/provider, UI/API, scheduler, event, or DB.**

---

## Update — Implementation 017 (Provider Adapter Seam)

[FACT] **Implementation 017** added the most dangerous output-out boundary safely: a **provider adapter seam inside `rendering`** that lets a future LLM provider **draft** human-facing text — as an **untrusted draft** that becomes a `RenderedMessage` **only** by passing the **unchanged mandatory `validateDraft`**. The provider replaces **only** the draft-text step the `FakeRenderer` performs; the validator, not the provider, remains the authority. It is **additive**: only the two `rendering` index files changed (additive exports); **no new top-level module**, **no documented blocker**.

[FACT] **Now true:**
- `rendering` gained (in `domain/` + `application/`): a constrained **`ProviderRenderingRequest`** (+ the `providerRenderingRequestFrom` construction guard), an untrusted **`ProviderDraft`**/`ProviderDraftOutcome`, a closed **`ProviderFailure`** catalog (10 values), a **`ProviderAdapter`** port, a deterministic **`FakeProviderAdapter`** (safe + each unsafe scenario + each failure), and a **`requestProviderRendering`** service returning a thin **`ProviderRenderOutcome`** around the existing `RenderOutcome`.
- The mandatory flow is **provider produces draft → `validateDraft({ draft, renderable, request })` → `RenderedMessage` only if validation passes**. The `rendered` branch's `message` is **exactly** the existing rendering path; a validation failure → **`provider-output-failed-validation`** with the underlying `RenderingFailure[]`.
- The **`ProviderRenderingRequest` is a constrained projection** of the authoritative `RenderingRequest` — only domain-approved fields (kind, domain-selected `VoiceMode`, allowed/forbidden claims, uncertainty/limitation requirements, traceability constraints, safe style/locale). It carries **no** raw private reasoning, chain-of-thought, mutable aggregate handle, override-voice/hide-uncertainty/ignore-validation field, arbitrary prompt text, or provider payload — those are **unrepresentable**.
- An **unsafe request is refused before any provider call** (`unsupported-style`/`unsupported-locale`/`unsafe-provider-request`); a provider failure maps to a closed reason (the network-flavored `provider-unavailable`/`-timeout`/`-rate-limited` are **fake-configurable**, no real semantics); **every** failure path **degrades to safe non-rendering**.
- A **`ProviderDraft` is untrusted** — it is **not** a `RenderedMessage`/`RenderedMessageRecord`/`Evidence`/`Observation`/`Understanding`/`AthleteDecision`/`DecisionSupport`; the provider **cannot** construct a `RenderedMessage` directly, **selects/changes no `VoiceMode`**, **creates no `TerminalOutput`/`Recommendation`**, and **persists/reviews/marks-display-eligible/delivers/emits-an-event/mutates nothing**.
- The seam **imports only its own `rendering` surfaces + read-only `decision-support` types**; **no module outside `rendering` imports it**; there is **no real provider SDK/API/network/`process.env`/prompt-templates-as-code** and **no `provider`/`llm`/`openai`/`anthropic`/`model` top-level module**.
- **Still absent (intentional):** a real provider/SDK + production prompts; provider-attempt persistence; provider event records; UI/API; a scheduler/retry layer; a production DB.

[FACT] **Distinctions this update makes explicit** (do not collapse):
- **provider draft ≠ rendered message** · **provider adapter ≠ renderer authority** · **provider draft ≠ domain truth**.
- **provider draft ≠ `TerminalOutput`/`Recommendation`/`Evidence`/`Observation`/`Understanding`/`AthleteDecision`** · **provider draft ≠ rendered-message record**.
- **provider success ≠ display eligibility** · **provider success ≠ delivery** · **provider failure ≠ domain invalidation** · **validation failure ≠ review rejection**.
- **fake provider ≠ real provider integration** · **provider seam ≠ SDK/API/network/prompt implementation** · **provider request ≠ raw private reasoning**.

[ASSUMPTION] The headline: **Aurora can now let a (fake, deterministic) provider draft rendered text behind the unchanged mandatory validator — the provider is only an untrusted draft source: it can never select voice, create a claim/terminal-output/message/record, bypass validation, persist/review/deliver, emit an event, or mutate the domain; an unsafe request is refused before the call and every failure degrades to safe non-rendering; and no real SDK/API/network/prompt or provider module exists.**

---

## Update — Implementation 018 (Provider Attempt Audit)

[FACT] **Implementation 018** added the last safety belt before a real provider: a **provider-attempt audit inside `rendering`** that records *what the seam did* — **auditability, not authority** — **without retaining raw drafts** or letting an attempt become source truth, evidence, or reasoning. It is **additive**: only the two `rendering` index files changed (additive exports); **no new top-level module**, **no documented blocker**.

[FACT] **Now true:**
- `rendering` gained (in `domain/` + `application/`): `ProviderAttemptRecordId`, **`ProviderAttemptStatus`** (closed; `requested`/`draft-produced` reserved), **`ProviderAttemptFailureReason`** (= `ProviderFailure | RenderingFailure`, real catalogs reused verbatim), **`ProviderDraftSummary`** (safe, no raw text), **`ProviderAttemptRecord`** (+ `ProviderAttemptRecordState`), a **`ProviderAttemptRecordRepository` port** + `InMemoryProviderAttemptRecordRepository`, and the observe-only **`auditProviderAttempt`** service.
- The audit **observes** an already-computed `ProviderRenderOutcome` and maps it: `rendered → validation-passed`; `failed`+`provider-output-failed-validation` → `validation-failed` (with the underlying `RenderingFailure[]`); `failed`+`unsupported-style`/`unsupported-locale`/`unsafe-provider-request` → `unsafe-request-blocked`; other `ProviderFailure` → `provider-failed`.
- **No raw draft retention:** `ProviderDraftSummary` is reason/count-based (`draftProduced`, `providerWarningCount?`, `validationFailureCount?`, `renderingFailureReasons?`, `providerFailureReason?`); **`rawDraftRetained` is the literal `false`**; reconstitution **rejects** any `draft`/`text`/`content`/`prompt` key or `rawDraftRetained: true`, and rejects unknown (payload-bag) keys + status/flag mismatches.
- **Observe-only:** `auditProviderAttempt` is a **pure mapping** — it **does not call** `requestProviderRendering` / `ProviderAdapter` / `validateDraft`. It records refs (renderable/source ref, terminal-output kind, domain `VoiceMode`), the provider adapter kind, the status, the reasons, and a safe draft summary.
- **Auditability, not authority:** a `ProviderAttemptRecord` is **not** a `ProviderDraft` / source truth / `Evidence` / `Observation` / `Understanding` / `AthleteDecision` / `DecisionSupport` / `TerminalOutput` / `RenderedMessage` / `RenderedMessageRecord`; it **creates no review/display-eligibility/delivery**, **appends no event**, **triggers no retry/scheduler/reprojection/reasoning**, and **mutates nothing**. A **validation failure is not domain invalidation**; provider success is not recommendation validation; provider failure does not weaken `SupportQuality`.
- The repository uses the Impl 010 pattern (deep-copy round-trip; mutation isolation; validated reconstitution); the audit imports only its own `rendering` surfaces + read-only `decision-support` *types*; **no module outside `rendering` imports it**; **no `event-recording` import / event-catalog expansion**.
- **Still absent (intentional):** a real provider/SDK + production prompts; provider/delivery event records; provider telemetry / model evaluation; UI/API; a scheduler/retry layer; a production DB.

[FACT] **Distinctions this update makes explicit** (do not collapse):
- **provider attempt record ≠ provider draft ≠ source truth** · **≠ `Evidence`/`Observation`/`Understanding`/`AthleteDecision`/`DecisionSupport`/`TerminalOutput`**.
- **provider attempt record ≠ `RenderedMessage`/`RenderedMessageRecord`** · **audit ≠ rendered-message persistence/review/display-eligibility/delivery/event record**.
- **audit observes outcome ≠ audit calls provider/validator** · **validation failure ≠ domain invalidation** · **provider success ≠ recommendation validation** · **provider failure ≠ support-quality weakening**.
- **provider attempt audit ≠ model evaluation / telemetry infrastructure** · **no raw draft retention ≠ no auditability**.

[ASSUMPTION] The headline: **Aurora can now remember what the provider seam did — a safe-summary, append-only `ProviderAttemptRecord` (status + real-catalog reasons, no raw draft) — by observing the outcome, never calling the provider/validator; the record is auditability not authority, retains no draft, triggers nothing downstream, and validated messages remain the only path to a `RenderedMessage`.**

---

## Update — Implementation 019 (Real-Provider-Ready Boundary)

[FACT] **Implementation 019** opened the most dangerous edge safely: it made a **real provider pluggable** behind the existing seam — accepting the *shapes* of secrets, network, prompt material, and provider errors — **without touching a real provider** and **without changing the authority model**. A real provider changes the **draft source**, not the gate. It is **additive**: only the two `rendering` index files changed (additive exports); the **synchronous Impl 017 seam is untouched**; **no new top-level module**, **no documented blocker**.

[FACT] **Now true:**
- `rendering` gained (in `domain/` + `application/`): `ProviderSecretRef` (+ `ProviderCredentialStatus`), a non-secret `ProviderClientConfig`, a structured `ProviderInstruction` (+ `providerInstructionFrom`), `ProviderClientRequest`/`ProviderClientResponse` (+ operational `ProviderClientMetadata`), a closed **`ProviderOperationalFailure`** (+ `toProviderFailure`), an **async `ProviderClientBoundary`** port, a deterministic **`FakeProviderClient`**, a **`RealProviderAdapter`**, and an async **`requestRealProviderRendering`** service.
- **Option B (additive async; sync seam untouched):** the existing synchronous `ProviderAdapter`/`FakeProviderAdapter`/`requestProviderRendering` (Impl 017) are **unchanged**; the new async path coexists. This is the **first `async`/`Promise` surface** in the codebase, isolated to the real-ready path.
- The async flow **reuses** the unchanged `providerRenderingRequestFrom` guard (unsafe requests refused **before** any client call), adds a **credential fast-path** (a non-`present` `ProviderSecretRef` fails safely before the call), calls the async client for an **untrusted draft**, then runs the **unchanged mandatory `validateDraft`**, and returns the **existing `ProviderRenderOutcome`** (validation failure → `provider-output-failed-validation` + the underlying `RenderingFailure[]`).
- **Secret refs, never secrets:** `ProviderSecretRef` is a status (`present`/`missing`/`invalid`) + an opaque ref; **no raw secret** in records/responses/errors, **no `process.env`**, none audited.
- **Structured instruction, not prompts:** `ProviderInstruction` is **derived** from the constrained request (kind, voice, allowed/forbidden claims, uncertainty/limitation/traceability constraints, safe style/locale); **no prompt-template file, no arbitrary prompt text, no chain-of-thought**.
- **Failure mapping, no expansion:** `ProviderOperationalFailure` (missing/invalid credential, unavailable, timeout, rate-limit, refused, empty, malformed, unsupported-config, unsafe-request) maps **down** to the **existing** `ProviderFailure` via `toProviderFailure`; the core catalog is **not expanded**. Every failure **degrades to safe non-rendering** with **no automatic retry**.
- **Auditability preserved, not automated:** the Impl 018 raw-free audit can observe the real outcome by **explicit composition**; the service **persists nothing automatically**. **Provider metadata is operational, not evidence.**
- **Still absent (intentional):** a real provider/SDK + production prompts + a real secret mechanism / network; provider/delivery event records; provider telemetry / model evaluation; UI/API; a scheduler/retry layer; a production DB.

[FACT] **Distinctions this update makes explicit** (do not collapse):
- **real-provider-ready boundary ≠ real provider integration** · **fake client ≠ real SDK/network** · **secret ref ≠ secret** · **provider instruction ≠ arbitrary prompt / production prompt template**.
- **async client boundary ≠ automatic retry/scheduler** · **failure mapping ≠ expanding provider authority** · **provider operational metadata ≠ evidence**.
- **provider draft ≠ rendered message** · **sync seam untouched ≠ duplicated authority** · **`ProviderRenderOutcome` ≠ provider-attempt persistence** · **real-provider-ready path ≠ delivery/eventing**.
- **provider success ≠ recommendation validation** · **provider failure ≠ domain invalidation**.

[ASSUMPTION] The headline: **Aurora is now real-provider-*ready* — a real provider is pluggable behind an additive async client boundary (proven with a deterministic fake in-process client; operational secret refs, structured instructions, failure mapping) that still ends at the unchanged mandatory `validateDraft` and returns the existing outcome; the sync seam is untouched, secrets/metadata stay operational, the audit stays raw-free, and nothing real (SDK/network/secret/prompt) is touched and no downstream side effect occurs.**

---

## Update — Implementation 020 (Concrete-Provider Adapter Shell)

[FACT] **Implementation 020** added the **first selected-provider adapter shell** behind the Impl 019 async `ProviderClientBoundary` — a concrete adapter that proves vendor-specific **serialization / parsing / error-mapping** against deterministic fixtures **without touching a real provider**. The provider target (**OpenAI**) was selected at the **doc/decision level only** (Tech Spec 020A); production/test code stays **vendor-neutral** (`concrete-provider-*`) so **no negative-capability guard was weakened and no vendor token leaks into a guarded file**. It is **additive**: only the `rendering/application/index.ts` export surface changed; **no new top-level module**, **no package/lockfile change**, **no documented blocker**.

[FACT] **Now true:**
- `rendering/application/` gained: a **`ConcreteProviderClient`** (implements the async `ProviderClientBoundary`), a pure **`serializeProviderInstruction`** (+ `ConcreteProviderRequestPayload`), a pure **`parseProviderResponse`** (+ `ConcreteProviderResponseShape`), and a pure **`mapProviderError`** (+ `ConcreteProviderErrorKind`/`ConcreteProviderErrorShape`). All four files live **inside `rendering/application`**.
- **Provider selected at decision level, neutral in code:** 020A records **OpenAI** as the first concrete-provider target; the code uses neutral `concrete-provider-*` names and a neutral `providerKind` (`"concrete"`). The vendor token never appears in a guarded provider file.
- **Disabled by default, fails safe:** with no injected transport, `ConcreteProviderClient.requestDraft` returns a safe failure (`provider-unavailable`) and does **no work** — **no live call path exists**. A non-`present` credential fails safe (`missing-credential`/`invalid-credential`) **before** any transport is consulted. The only non-default behavior is a **deterministic in-process fixture transport** used **only by tests** (not a network call).
- **Serializer is structured and safe:** `serializeProviderInstruction` is a pure projection of `ProviderInstruction` into a neutral provider-shaped payload carrying **only** safe constraints (terminal-output kind, voice, style, locale, allowed/forbidden claims, uncertainty-visible requirement, limitations, traceability, maxLength). There is **no field** for an arbitrary prompt / chain-of-thought / hidden reasoning / voice override / secret — they are unrepresentable; **no prompt-template file, no `src/prompts`**.
- **Parser returns untrusted draft + operational metadata only:** `parseProviderResponse` extracts only draft text (untrusted) + operational metadata (provider kind / finish reason / token count / latency); empty → `provider-returned-empty-response`, malformed → `provider-returned-malformed-response`; it retains **no raw provider payload** and **never** creates a `RenderedMessage`.
- **Error mapper maps down, no expansion:** `mapProviderError` maps provider-shaped errors (live-disabled, missing/invalid credential, network-unavailable, timeout, rate-limit, refusal, empty, malformed, unsupported-config, unknown) to the existing **`ProviderOperationalFailure`**, which the existing `toProviderFailure` maps **down** to the existing **`ProviderFailure`** (**not expanded**); an unknown error fails safe (`provider-unavailable`) and leaks no raw payload/secret.
- **Validator + audit unchanged:** a concrete-client draft becomes a `RenderedMessage` **only** via the unchanged `requestRealProviderRendering` → **`validateDraft`**; the Impl 018 raw-free audit observes the outcome by **explicit composition** (no automatic persistence; `rawDraftRetained` literal `false`).
- **No new dependency / network / secret / side effect:** **no SDK installed** (`package.json`/`package-lock.json` unchanged; a test asserts devDeps remain `typescript` + `@types/node`); **no network, no `process.env`, no raw secret, no retry/scheduler/event bus**; **no record/review/display-eligibility/delivery/event/domain mutation**.
- **Still absent (intentional):** a **live provider call** (the shell is disabled by default; live calls are a later slice), a real SDK/secret-mechanism/network, production prompts, provider/delivery event records, provider telemetry / model evaluation, UI/API, a scheduler/retry layer, and a production DB.

[FACT] **Distinctions this update makes explicit** (do not collapse):
- **provider target selected ≠ live provider integration** · **vendor doc decision ≠ vendor token in production code** · **concrete-provider shell ≠ SDK client**.
- **disabled-by-default client ≠ live-call adapter** · **fixture transport ≠ network** · **secret ref ≠ raw secret**.
- **serializer ≠ arbitrary prompt** · **serializer ≠ production prompt template** · **parser output ≠ rendered message** · **provider metadata ≠ evidence**.
- **provider-shaped error ≠ domain invalidation** · **`ProviderOperationalFailure` mapping ≠ expanding `ProviderFailure`** · **provider success ≠ persistence/review/delivery** · **composed audit ≠ automatic audit persistence**.
- **no SDK dependency ≠ no provider plan** · **selected-provider shell ≠ weakening negative-capability guards**.

[ASSUMPTION] The headline: **Aurora now has the first selected-provider adapter shell behind the async client boundary — OpenAI chosen at the doc/decision level while code stays vendor-neutral (`concrete-provider-*`), a disabled-by-default client plus deterministic serializer/parser/error-mapper — adding no SDK, no network, no live call, no API keys, no env reads, no raw secrets, no prompt templates, no automatic persistence, no review/display/delivery/event side effect, and no domain mutation; provider output stays untrusted and must pass `validateDraft`. This is a selected-provider *shell*, not live provider integration.**

---

## Update — Implementation 021 (Opt-In Live-Provider Boundary)

[FACT] **Implementation 021** opened the **first live-call edge** — and kept it **fail-closed**: a provider call can now be *real* network I/O, but only behind the existing async `ProviderClientBoundary`, only under explicit opt-in, and only through **native `fetch` isolated in a single approved transport file**. **No SDK was installed, no dependency/lockfile changed, no environment variable is read, no real secret mechanism exists, and the default test suite / CI make no live call and require no credentials.** A live call is **operational I/O; it does not change the authority model** — `validateDraft` stays the gate, the audit stays raw-free, provider output stays untrusted. It is **additive**: only the `rendering/application/index.ts` exports and **two rendering network guards** changed (surgically); **no new module**, **no documented blocker**.

[FACT] **Now true:**
- `rendering/application/` gained: a **`LiveCallPolicy`** (injected value object; `disabled()` / `enabled({timeoutMs})`), a **`ProviderCredentialResolver`** port (+ opaque `ProviderCredentialToken`), a deterministic **`StaticProviderCredentialResolver`** (test/composition only), a **`LiveProviderHttpTransport`** (the only network-token file), and a **`LiveProviderClient`** (implements the async `ProviderClientBoundary`).
- **Disabled by default, fail-closed:** `LiveCallPolicy.disabled()` is the safe default; opt-in is **explicit** via the injected policy (never inferred from the environment; no global state). The client **fails closed before any transport call** when the policy is disabled, or the credential is missing/invalid (→ safe `provider-unavailable` / `missing-credential` / `invalid-credential`).
- **Native HTTP, isolated, no SDK:** `LiveProviderHttpTransport` is the **only** production file permitted a network token (`fetch` + `AbortSignal.timeout`, behind an **injected endpoint** — no hard-coded vendor URL). **No SDK installed; `package.json`/`package-lock.json` unchanged.** The transport does no retry, no persistence, no validation, no domain mutation, and leaks no secret in errors.
- **Credentials never become domain data:** the credential resolver is **injected**; the static resolver is deterministic/test-only with a **non-secret sentinel**; **no env resolver exists and no `process.env` is read**; the credential is used transiently in the request header only — never persisted/audited/logged/in errors.
- **`LiveProviderClient` is a sibling of `ConcreteProviderClient`** (async network transport vs sync fixture), **reusing** the unchanged `serializeProviderInstruction` / `parseProviderResponse` / `mapProviderError`. It **never calls `validateDraft`** — validation stays owned by `requestRealProviderRendering(...)`; provider output is an **untrusted draft**.
- **Failure mapping, no expansion:** transport conditions (live-disabled, missing/invalid credential, timeout, rate-limit, network-unavailable, refusal, empty, malformed, unsupported-config, unknown) map onto the **existing** `ProviderOperationalFailure → ProviderFailure` — **neither catalog expanded**; unknown fails safe (`provider-unavailable`) leaking nothing.
- **Audit raw-free, nothing automatic:** a live attempt is observed by the Impl 018 `auditProviderAttempt` only by **explicit composition** (no automatic persistence; `rawDraftRetained` literal `false`); there is **no record/review/display-eligibility/delivery/event/domain-mutation** side effect and **no retry/scheduler/event bus**.
- **Surgical guard exception:** the Impl 014 broad rendering scan and the Impl 017 `/provider-/` scan now allow a network token **only** in `live-provider-http-transport.ts` (each with a positive "exactly one network file" assertion); **vendor / SDK / env tokens stay forbidden everywhere**, including that file. The Impl 019/020 guards were **not touched**.
- **Untouched:** the synchronous provider seam, `FakeProviderAdapter`, `FakeProviderClient`, and `ConcreteProviderClient`.
- **Still absent (intentional):** a **production secret/env resolver** (env reads deferred), a real SDK, **production live-call rollout** (the boundary is *capable*, not *deployed*), production prompts, provider/delivery event records, telemetry / model evaluation, UI/API, a scheduler/retry layer, a production DB.

[FACT] **Distinctions this update makes explicit** (do not collapse):
- **live-provider boundary ≠ production rollout** · **opt-in capability ≠ enabled-by-default live call** · **native HTTP transport ≠ SDK dependency** · **one approved network file ≠ broad network permission**.
- **credential resolver port ≠ real secret manager** · **static resolver ≠ production credential mechanism** · **injected credential token ≠ persisted secret** · **fetch exception ≠ weakened vendor/env/SDK guards**.
- **disabled policy ≠ transport invocation** · **missing credential ≠ transport invocation** · **invalid credential ≠ domain failure**.
- **provider payload ≠ arbitrary prompt** · **transport response ≠ rendered message** · **parsed draft ≠ validated message** · **provider metadata ≠ evidence** · **transport failure ≠ domain invalidation**.
- **composed audit ≠ automatic audit persistence** · **live-capable client ≠ retry/scheduler/event bus** · **live-provider success ≠ persistence/review/display/delivery**.

[ASSUMPTION] The headline: **Aurora can now make a *real* provider call — but only when explicitly enabled, behind the unchanged `ProviderClientBoundary` and the unchanged `validateDraft`, with native `fetch` sealed in one approved file, credentials injected (never env-read, never persisted), the audit raw-free, and the default suite/CI credential-free and non-live. A live call is operational I/O; it changes the draft source, never the authority model. This is a live-call-*capable* boundary, not a production live-provider rollout.**

---

## How to Read This Document

| Tag | Meaning |
|---|---|
| **[FACT]** | Follows necessarily from the implemented code, tests, or an accepted spec/model. |
| **[DECISION]** | A consolidation commitment recorded here. |
| **[ASSUMPTION]** | A stance taken for this review. |
| **[QUESTION]** | Open; carried forward, does not block. |

---

## 1. Executive Summary

[FACT]
- **The reasoning core is implemented end-to-end.** All five ladder stages exist in code (`observation → reasoning → understanding → decision-support`) and Implementation 006 composes them into one demonstrated chain.
- **The first full output is `Reflection`, not `Recommendation`.** The end-to-end integration test proves a complete chain that lands on a modest voice.
- **The system demonstrates restraint *by construction*.** Even with complete traceability and clean gates, a single chain tops out at Reflection because the understanding ceiling caps the voice — restraint is structural, not a runtime preference.
- **The current suite proves the core can compose without overreaching.** 145 tests pass, including module-boundary tests, negative-capability ("defining") tests, and the end-to-end Reflection proof.
- **Persistence exists now, but only as ports + in-memory adapters (Impl 010):** validated `toState()`/`reconstitute()` + repository ports for the six persisted boundaries, all in-memory. **Production persistence remains absent** — no **production DB/ORM/schema/migrations**, no **event bus**, no **cache**, no **`src/infrastructure`**, no **projection repository**.
- **Event/outcome records exist now, as a dependency-neutral append-only log (Impl 011):** the `event-recording` module records *what happened* — one ref-only `DomainEventRecord` (occurrence/outcome) over a closed catalog, with a `TraceabilityEnvelope` and an append-only in-memory log/repository — **complementing the aggregate repositories, not replacing them**. It remains **non-command, non-bus**: no event bus, publish/subscribe, handlers, async delivery, serialization tech, or **event sourcing**.
- **Reprojection exists now, as a neutral check-only harness (Impl 012):** a recompute-and-compare *safety mechanism* (test-support under `__tests__/reprojection-harness/`, **not a production module**) that re-derives `UnderstandingAssessment` through the owning module, recalculates freshness, detects candidates from event records, and **reports** drift/findings — **mutating nothing, executing no event, rebuilding nothing from the log, and turning no projection into truth**. No scheduler, event sourcing, or projection repository exists.
- **A real "data in" boundary exists now (Impl 013):** an `observation`-owned **Manual Input Adapter** records manually supplied input faithfully as an `ObservationSet` (via `recordObservationSet`, persisted through `ObservationSetRepository`), preserving provenance/source/quality and verbatim words, representing missing data explicitly, and rejecting the unrepresentable — **without interpreting, detecting a `Signal`, reasoning, mutating athlete records, or triggering downstream effects**. No UI/API/LLM/external integration exists; `observation` imports no `event-recording`.
- **A real "output out" boundary exists now (Impl 014):** a downstream **`rendering` module** turns a domain-approved `TerminalOutput` into human-facing text via a **deterministic fake renderer + a mandatory validator** — preserving voice/uncertainty/limitations/freshness/traceability/agency and **refusing** any draft that escalates voice, strengthens a claim, hides uncertainty, or invents a fact (safe non-render on failure). Generated text is **presentation, not authority**: rendering selects no voice, creates no `TerminalOutput`, mutates no aggregate, and writes no event. **No real LLM provider/prompt/UI/API/external call** exists.
- **The output-out cycle is now auditable (Impl 015):** **inside `rendering`**, an append-only **`RenderedMessageRecord`** + an append-only **`RenderReview`** (display-safety only) + a **derived `DisplayEligibility`**, behind a **repository port + in-memory adapter**. Persistence/review improve **auditability and display safety only** — a record is **never** domain truth, approval **never** strengthens the domain, rejection **invalidates nothing**, failed attempts are **never** display-eligible, and nothing triggers an event or delivery. **No rendered-output events, no production DB, no delivery/UI/API/provider.**
- **A delivery/exposure boundary exists now (Impl 016):** a downstream **`delivery` module** exposes a *display-eligible* `RenderedMessageRecord` to a **deterministic test-only sink** and records the attempt as an auditable **`DeliveryRecord`** (repository port + in-memory adapter). Delivery **verifies** eligibility via `rendering`'s `displayEligibilityOf(record)` (it never re-derives it), calls the sink **only** when eligible *and* the target is the supported `test-sink`, and **blocks** every ineligible/unsupported request. **Delivery is exposure and auditability only** — **success is not evidence, failure is not domain invalidation**; it mutates no rendered record/aggregate, emits no event, and triggers no reasoning/reprojection/retry. **No real provider/channel, UI/API, scheduler/retry, event bus, delivery events, or production DB.**
- **A provider adapter seam exists now (Impl 017):** **inside `rendering`**, a deterministic **fake provider** may **draft** human-facing text — an **untrusted `ProviderDraft`** that becomes a `RenderedMessage` **only** by passing the **unchanged mandatory `validateDraft`** (the validator, not the provider, is the authority). The provider replaces **only** the draft-text step; an unsafe request is refused **before** the call; every failure/unsafe draft **degrades to safe non-rendering**. The provider **selects no voice, creates no `TerminalOutput`/`Recommendation`/`RenderedMessage`/record, and persists/reviews/marks-display-eligible/delivers/emits/mutates nothing**. **No real SDK/API/network/prompt-templates and no `provider`/`llm` module exist.**
- **A provider-attempt audit exists now (Impl 018):** **inside `rendering`**, an append-only **`ProviderAttemptRecord`** (repository port + in-memory adapter) records *what the seam did* by **observing** a `ProviderRenderOutcome` — a **safe summary** (status + reasons reusing the real `ProviderFailure`/`RenderingFailure` catalogs) with **no raw draft retention** (`rawDraftRetained` literal `false`). The audit **does not call** the provider/`requestProviderRendering`/`validateDraft`; it **creates no `RenderedMessage`/record/review/display-eligibility/delivery**, **appends no event**, **triggers no retry/reprojection/reasoning**, and **mutates nothing**; a **validation failure is not domain invalidation**. It is **auditability, not authority** — not model evaluation, not telemetry. **No provider event / event-catalog expansion / real SDK exist.**
- **A real-provider-ready boundary exists now (Impl 019):** **inside `rendering`**, an **additive async** path makes a real provider **pluggable** while **changing only the draft source** — the **synchronous Impl 017 seam is untouched**. An async **`ProviderClientBoundary`** (proven with a deterministic **`FakeProviderClient`**) takes operational **`ProviderSecretRef`s** (never raw secrets; no `process.env`), structured **`ProviderInstruction`** material (derived, not a prompt template), and maps a closed **`ProviderOperationalFailure`** **down** to the existing `ProviderFailure` (**not expanded**). `requestRealProviderRendering` **reuses** the unchanged `providerRenderingRequestFrom` guard + the mandatory **`validateDraft`** and returns the existing `ProviderRenderOutcome`; every failure **degrades to safe non-rendering** with **no automatic retry**, **no automatic persistence**, and **no review/display/delivery/event/domain-mutation**. **Real-provider-*ready*, not real-provider-*integrated*:** no real SDK/API/network/secret/prompt exists.
- **A concrete-provider adapter shell exists now (Impl 020):** **inside `rendering/application`**, the **first selected-provider adapter** behind the async `ProviderClientBoundary` — a **`ConcreteProviderClient`** (disabled by default → safe failure, no work; no live call path) plus a pure **serializer / parser / error-mapper**. The provider target (**OpenAI**) is selected at the **doc/decision level (020A) only**; code stays **vendor-neutral** (`concrete-provider-*`), so **no guard was weakened and no vendor token leaks into a guarded file**. The serializer projects only safe constraints (no arbitrary prompt / chain-of-thought field); the parser returns an **untrusted draft + operational metadata** (no raw payload, no `RenderedMessage`); the error mapper maps provider-shaped errors **down** to the existing `ProviderOperationalFailure` → `ProviderFailure` (**not expanded**). A draft becomes a message **only** via the unchanged `validateDraft`; the audit stays raw-free by explicit composition. **No SDK/package change, no network, no `process.env`, no raw secret, no prompt template, no retry/scheduler, and no record/review/display/delivery/event/domain side effect.** **Selected-provider *shell*, not live integration.**
- **An opt-in live-provider boundary exists now (Impl 021):** **inside `rendering/application`**, the **first live-call edge** behind the async `ProviderClientBoundary` — a **`LiveProviderClient`** driven by an injected **`LiveCallPolicy`** (disabled by default; explicit opt-in; no env inference), an injected **`ProviderCredentialResolver`** (deterministic `StaticProviderCredentialResolver` for tests; **no env resolver, no `process.env`**), and a single **`LiveProviderHttpTransport`** — the **only** file permitted a network token (native `fetch` behind an injected endpoint; **no SDK, no dependency/lockfile change**). It **fails closed before any transport call** when the policy is disabled or the credential is missing/invalid; **reuses** the unchanged serializer/parser/error-mapper; **never calls `validateDraft`** (validation stays with `requestRealProviderRendering`); provider output stays **untrusted**. Failures map onto the existing `ProviderOperationalFailure → ProviderFailure` (**not expanded**); the audit stays raw-free by explicit composition. **Default tests / CI make no live call and need no credentials**; the network-guard exception is **surgical** (one approved file; vendor/SDK/env stay forbidden everywhere); the sync seam, `FakeProviderAdapter`, `FakeProviderClient`, and `ConcreteProviderClient` are untouched; **no record/review/display/delivery/event/domain side effect, no retry/scheduler**. **Live-call-*capable*, not a production rollout.**
- **What does not exist yet:** no UI, API, **production DB/persistence**, **real LLM provider/SDK** (a deterministic rendering boundary — Impl 014 — a fake **provider adapter seam** behind the unchanged validator — Impl 017 — **provider-attempt audit** — Impl 018 — a **real-provider-*ready* async client boundary** — Impl 019 — a **selected-provider adapter shell** (neutral code, disabled by default, deterministic serializer/parser/error-mapper) — Impl 020 — and an **opt-in live-provider boundary** (fail-closed, native `fetch` isolated in one approved file, injected credential resolver) — Impl 021 — exist, but **no SDK/dependency, no env reads, no real secret mechanism, no default/CI live call, and no production rollout**), **real delivery channel/provider** (a delivery boundary with a deterministic test sink exists — Impl 016 — but no email/SMS/push/web channel), event bus, notification layer, scheduler/retry layer, provider/delivery event records, provider telemetry / model evaluation, Garmin/FIT adapter, or production orchestration service. The `athlete` module now holds **Purpose (Impl 007) + AthleteDecision (Impl 009)** — but **full Athlete** (state/capacity/constraints/path-memory), a **full `DecisionOutcome` object**, a **pattern engine**, and any **compliance/outcome-based validation** are still absent. These are **intentional absences** (see §6), not gaps left by mistake.

[ASSUMPTION] The one-sentence claim this review defends: **Aurora can run the full reasoning core end-to-end and still refuse to overreach.**

---

## 2. Implemented Core Flow

[FACT] The implemented flow, stage by stage. "Refuses to" names what is *unrepresentable or rejected* at that stage, not merely untested.

```
ObservationSet → Observation → ContextualizedObservation → Signal/SignalRejection
   → EvidenceCase → Hypothesis lifecycle → UnderstandingProfile update
   → UnderstandingAssessment → DecisionSupportCase → Terminal Output (DecisionSupport: Reflection)
```

| Step | Owning module | Implemented object(s) | Protected invariant | Handoff downstream | Refuses to |
|---|---|---|---|---|---|
| **ObservationSet / Observation** | `observation` | `ObservationSet`, `MeasuredObservation`, `SubjectiveObservation`, `MissingDataObservation`, `Provenance`, `Source`, `ObservationQuality` | Provenance/quality born at capture and immutable; corrections supersede, never overwrite; incompleteness explicit | raw, provenance-bearing observations | hold any interpretation/meaning field |
| **ContextualizedObservation** | `observation/signal` | `ContextualFrame`, `ContextualizedObservation`, `TraceToObservation` | Frame is context, not conclusion; the original is never mutated; trace to the set/observation is born here | a contextualized observation | assign meaning or direction |
| **Signal / SignalRejection** | `observation/signal` | `Signal`, `SignalRejection`, `SignalDetectionPolicy` (`expectedRangeDeviationPolicy`) | Relevance-without-meaning; every outcome is a Signal *or* an auditable rejection (nothing silently dropped); quality/provenance travel forward | a `Signal` (or recorded rejection) | become evidence; resolve a source conflict |
| **EvidenceCase** | `reasoning` | `EvidenceCase`, `TraceToSignal`, `EvidenceDirection` | An `EvidenceCase` is created **only** by `Hypothesis.attachEvidence`; a `SignalRejection` can never become evidence | evidence attached to a claim | exist standalone; outside a hypothesis |
| **Hypothesis lifecycle** | `reasoning` | `Hypothesis` (aggregate root), `Falsifier`, `ClaimConfidence`, lifecycle states/transitions | Always falsifiable; calibrated, defeasible confidence; no silent transitions; never "certain" | a settled lifecycle **outcome** | become a fact; be certain |
| **UnderstandingProfile update** | `understanding` | `UnderstandingProfile` (aggregate), `UnderstandingDimension`, `ReasoningOutcome` adapter, `SurvivedChallenge`, `Surprise`, `Staleness`, `Fragility` | Per-dimension, never global; promotion only by survived challenge across ≥2 distinct conditions; population/repetition never promote; history never deleted | a per-dimension level + trail | read raw signals; store claim confidence; go global |
| **UnderstandingAssessment** | `understanding` | `UnderstandingAssessment`, `SafeVoiceCeiling`, `deriveSafeVoiceCeiling` | Read-only projection; staleness/fragility can only **lower** the ceiling; the ceiling is **not** a `VoiceMode` | a `SafeVoiceCeiling` (max assertiveness) | select a voice; recommend |
| **DecisionSupportCase** | `decision-support` | `DecisionOpportunity`, `DecisionSupportCase` (aggregate), 5 gates, `TraceabilityVerification`, `VoiceSelectionPolicy`, `VoiceMode`, terminal outputs, `SupportQuality` | No output before gates run; voice gated not derived; Recommendation needs complete trace + confident ceiling; agency preserved; `AthleteDecision` referenced, never owned | a terminal output | own the decision; author claims; command |
| **Terminal Output** | `decision-support` | `DecisionSupport` (with `VoiceMode`), `Inquiry`, `Withholding` | `DecisionSupport` requires `preservesAgency: true` (literal); `Inquiry` is not a `VoiceMode`; `Withholding` carries an auditable reason | (the athlete-facing edge) | emit a command; hide uncertainty |

---

## 3. Module Inventory

### `observation` *(intake + Manual Input Adapter — Impl 013)*
[FACT] Implements `ObservationSet` (aggregate), `MeasuredObservation` / `SubjectiveObservation` / `MissingDataObservation`, `Provenance` / `Source` / `ObservationQuality`, `ContextualFrame` + `contextualize`, `Signal` / `SignalRejection`, and `expectedRangeDeviationPolicy` (the `SignalDetectionPolicy`). Coordinators: `recordObservationSet`, `detectSignals`. **Plus the Manual Input Adapter (Impl 013):** `ManualInputSubmission`, `ManualInputEntry`, `ManualInputIngestionOutcome` + closed rejection/limitation/quality catalogs, and `ingestManualInput({ submission, observationSetRepository })`.
- **Clarify:** `observation` still does **not reason** — it imports only `shared-kernel`; `Hypothesis`/`Evidence`/`Impact`/`Understanding`/`DecisionSupport` are not reachable here. **A `Signal` is not `Evidence`** — it asserts only *possible relevance to a future reasoning question*, with no slot for cause/impact/state.
- **Manual Input Adapter (Impl 013):** the first real **"data in"** boundary — it records manual input **faithfully as an `ObservationSet`** (via `recordObservationSet`, persisted through `ObservationSetRepository`), preserving **verbatim subjective `words`**, **explicit missing data**, and **provenance/quality** (`source: "manual"`). It returns `accepted`/`partially-accepted`/`rejected` (rejection saves nothing); `measured-value` is reserved; an `athlete-decision-report` is recorded **only as a subjective observation** (no `AthleteDecisionRecord` write). It **detects no `Signal`**, **infers nothing**, and **imports no downstream module or `event-recording`** — the optional `ObservationSetRecorded` is composed only in a neutral harness from a ref-only candidate. **Manual input is source material, never meaning.**

### `reasoning`
[FACT] Implements `Hypothesis` (aggregate root) owning `EvidenceCase` (entity), `EvidenceDirection`, `ClaimConfidence`, `Falsifier`, and the lifecycle (`proposed → supported/weakened/contradicted/falsified/retired/promoted-to-working-knowledge`). Coordinators: `openHypothesis`, `attachSignalAsEvidence`, `transitionHypothesis`.
- **Clarify:** a `Hypothesis` is **not fact** and never reaches a "certain"/"proven" state; **confidence is not certainty** (it is calibrated and defeasible); **an `EvidenceCase` exists only inside a `Hypothesis`** — `createEvidenceCase` is deliberately *not* exported, and a `SignalRejection` is rejected as evidence at runtime and by type.

### `understanding`
[FACT] Implements `UnderstandingProfile` (aggregate, per-dimension), `UnderstandingDimension`, `UnderstandingLevel` (Unknown < Thin < Working < Trusted < Mature), the `ReasoningOutcome` anti-corruption adapter (`reasoningOutcomeFrom`), `SurvivedChallenge`, `Surprise`, `Staleness`, `Fragility`, `UnderstandingAssessment`, and `SafeVoiceCeiling`. **Plus projection freshness (Impl 008):** `ProjectionFreshness`/`Status`, `StalenessReason`, `ProjectionSourceRef`/`Kind`, `ProjectionTrace`, `ProjectionLimitations`, `RefreshTrigger`/`Kind`, `ProjectionRefreshDecision`/`Kind`, `ProjectionRefreshPolicy`, `clampCeilingByFreshness`, `applyFreshness`. Coordinators: `updateUnderstandingFromOutcome`, `produceUnderstandingAssessment` (now `at`-aware), `markUnderstandingStale`.
- **Clarify:** **claim confidence is not understanding** — the adapter deliberately drops `ClaimConfidence` and raw `EvidenceCase`s; **repetition does not promote** — promotion requires survived challenge across ≥2 distinct conditions; **`SafeVoiceCeiling` is not a `VoiceMode`** — it is understanding's own vocabulary for *maximum permitted assertiveness*.
- **Projection freshness (Impl 008):** `UnderstandingAssessment` is a **projection/read model** carrying explicit `freshness` (`current`/`stale`/`partial`/`invalid`/`unknown`), `derivedAt`, and `sourceRefs`. Non-current freshness can only **lower** the ceiling; `invalid`/`unknown` clamp it to `none`. The refresh policy is **pure, selective, conservative**; **refresh = recompute** (new view, old one auditable, never mutated). Freshness reaches `decision-support` **only via the clamped `safeVoiceCeiling`** — no `decision-support` change.

### `decision-support`
[FACT] Implements `DecisionOpportunity`, `DecisionSupportCase` (aggregate root), the five gates (`EvidenceGate`, `UnderstandingGate`, `PurposeGate`, `RiskGate`, `AgencyGate`), `TraceabilityVerification` (`verifyTraceability`), `VoiceSelectionPolicy` (`selectTerminalOutput`), `VoiceMode`, terminal outputs (`DecisionSupport`/`Inquiry`/`Withholding`), `RiskAssessment`, `PurposeContext`, `SupportQuality`, and `AthleteDecisionRef`. Coordinators: `openDecisionSupportCase`, `evaluateDecisionSupportCase`, `recordAthleteDecisionRef`.
- **Clarify:** **voice is gated, not derived** — `VoiceSelectionInputs` carries **no claim-confidence field**, so confidence→voice is unrepresentable; **`Recommendation` is hard to construct** — it requires `confident` ceiling + complete traceability + all gates passing; **`AthleteDecision` is referenced, not owned** — only an `AthleteDecisionRef` field exists, recorded after the fact, and `SupportQuality` reflects gate integrity, not outcome.

### `athlete` *(Purpose — Impl 007 · AthleteDecision — Impl 009)*
[FACT] Implements a thin `Athlete` aggregate root (+ `AthleteId`) owning an **append-only `PurposeHistory`** of immutable `PurposeVersion`s; `Purpose`/`DeclaredPurpose`, `PurposeStatus`, `PurposeSource`, `PurposeChangeReason`; `PurposeVersionRef`; `PurposeChanged` (a derived/returned value, **no event bus**); `PurposeReinterpretationStatus` + result value type (**type only — no engine**); a `RevealedPurposeSignal` placeholder. **Plus the AthleteDecision slice (Impl 009):** `AthleteDecisionId`, `AthleteDecision` (+ athlete-local `AthleteDecisionRef`, `DecisionReportSource`), `DecisionChoice`, `DecisionRationale`, `DecisionContext`, `DecisionOutcomeRef`, `AthleteDecisionRecord` (append-only; `amend`/supersede). Coordinators: `declarePurpose`, `changePurpose`, `recordAthleteDecision`, `amendAthleteDecision`. Imports **only `shared-kernel`**.
- **Clarify (Purpose):** **`Athlete` owns declared context, not inferred truth** — no state/capacity/readiness/fatigue/constraints/path-memory; **purpose changes the future lens, it does not repaint the past** — history is append-only, prior reasoning is never rewritten; **`athlete` reaches no downstream module** — purpose flows to `decision-support` (as `PurposeContext`) and to `understanding` (as selective staleness) only through **neutral harness/application adapters**; **declared ≠ revealed**.
- **Clarify (AthleteDecision, Impl 009):** the decision is **athlete-owned** and **append-only** (corrections amend/supersede, original retained); `DecisionSupportCase` records **only an `AthleteDecisionRef`** (referenced, never owned); a `DecisionChoice` keeps a **modification** as free text (**no binary compliance**), and `divergedFromSupport` is **neutral fact, not a score**; there is **no compliance/obedience/shame/reward field**; `DecisionOutcomeRef` is a **reference only** (no full outcome object); the decision **re-enters as a `SubjectiveObservation`** via a neutral adapter (`athlete` never imports `observation`) — **never** directly as Signal/Evidence/Understanding.

### Persistence (Impl 010) — across modules
[FACT] Each persisted boundary gained an additive `toState()` / `static reconstitute(state)` and a module-owned repository **port** + **in-memory adapter** (in `application/`): `observation/ObservationSetRepository`, `reasoning/HypothesisRepository`, `understanding/UnderstandingProfileRepository`, `decision-support/DecisionSupportCaseRepository`, `athlete/AthleteRepository`, `athlete/AthleteDecisionRecordRepository`.
- **Clarify:** **persistence is a boundary around aggregates, not a driver of the domain** — `reconstitute` **validates invariants and rejects invalid state** (never a raw field bag); the in-memory adapter **stores deep-copied state, not live references**, so loads are independent (mutation isolation); `PurposeHistory` persists **through `Athlete`**; there is **no projection repository** (freshness survival is a test helper); and **no technology is chosen** (no DB/ORM/schema/event-bus/cache/infrastructure).

### `event-recording` *(Impl 011 — append-only event/outcome records)*
[FACT] Implements a **dependency-neutral** occurrence log: `DomainEventRecord` (+ `DomainEventRecordId`), `DomainEventType` (closed 26-type catalog) + `DomainEventCategory` (`occurrence`/`outcome`), `TraceabilityEnvelope` (+ `ProjectionFreshnessStatus` label), `EventPayloadRef` (+ `EventArtifactKind`), `CausationRef`, `CorrelationRef`, `EventActor`, `DomainEventRecordLog`; and in `application/`, the `DomainEventRecordRepository` **port** (`append`/`findById`/`all`/`findByCorrelation`) + `InMemoryDomainEventRecordRepository`. Imports **only `shared-kernel`**.
- **Clarify:** the module records **what happened**, never **what should execute** — a record **is not a command** (appending mutates nothing), **is not an aggregate / projection / source truth**, and **does not replace** the Impl 010 repositories (those answer *what is the aggregate now?*). Payloads are **ref-only** (`kind`/`id`/`role?`/`ownerModule?`) — **copied aggregate state and arbitrary bags are unrepresentable**; the log is **append-only** (corrections are new records; duplicate ids rejected); the in-memory repository **stores copies, not live references** and has **no** update/delete/dispatch/publish/subscribe surface; `toState()`/`reconstitute()` **re-validate** the closed catalog (type↔module/category, required refs, `occurredAt ≤ recordedAt`) and execute nothing on rehydrate. **No domain module imports `event-recording`; the catalog stays out of `shared-kernel`; no event bus / event sourcing / serialization tech exists.**

### `reprojection-harness` *(Impl 012 — neutral check-only recompute-and-compare seam, NOT a module)*
[FACT] Lives under `src/modules/__tests__/reprojection-harness/` as **test-support / coordination**, not a production module: `runReprojection` + `ReprojectionRun`/`Result`/`Finding`/`Mode`/`Target`/`InputSet`, a **pure** event→candidate map, and an `understanding` recompute adapter. It is the neutral cross-module coordinator (like the existing purpose/decision adapters); it imports the modules it coordinates, and **no production module imports it**.
- **Clarify:** the harness **recomputes** derived views *only through the owning module's existing functions* (`produceUnderstandingAssessment`, `applyFreshness`) — it **coordinates, never reasons**; **recalculates** freshness (never promotes it, never raises a ceiling); reads **event records as candidates/context only** (never replayed/executed, never rebuilding an aggregate — empty repos → `event-record-only`/`missing-source`); **reports drift** (never overwrites); and in `check-only` (default and only mode; `refresh-derived`/`mark-stale` reserved and throwing) **mutates no repository, appends no record, and creates no `TerminalOutput`/recommendation/`SupportQuality` rewrite/`Purpose` overwrite/`DomainEventRecord`**. **Repositories remain the source of aggregate state; event records remain occurrence history; reprojection recomputes views and reports findings — none replaces the others.**

### `rendering` *(Impl 014 — deterministic downstream rendering boundary)*
[FACT] Implements `src/modules/rendering/`: `RenderableDomainOutput` (+ module-local `RenderableFreshness`) + `renderableFromTerminalOutput`, `RenderingRequest` (+ closed `SAFE_STYLES`/`SUPPORTED_LOCALES`), `RenderedMessage`/`RenderOutcome`, a closed **12-value `RenderingFailure`** catalog, the `RenderingPolicy` cue vocabularies + predicates, a **mandatory `RenderingValidator`** (`validateDraft`), and a deterministic **fake renderer** (`fakeRender`/`render`). Imports **only `shared-kernel` + read-only `decision-support` types**.
- **Clarify:** rendering is **downstream presentation, never reasoning and never `decision-support`** — it reads a **read-only projection** of a completed `TerminalOutput` and owns **phrasing only** (`decision-support` owns voice + terminal output). The **fake renderer is deterministic** (no provider/model/randomness); **every draft must pass the mandatory validator** (the validator, not the renderer, is the guarantee). Voice may **match or soften, never escalate**; `Inquiry` stays a question; `Withholding` stays a refusal; `Recommendation` preserves conditions/uncertainty/traceability/agency; invented facts/citations, hidden uncertainty/limitations, and unsafe domain language are **rejected** (safe non-render). A `RenderedMessage` is **not** domain authority (`≠ Evidence/Observation/Understanding/AthleteDecision`); rendering **mutates no aggregate, writes no event**, and **no domain module imports `rendering`**. **No real LLM provider / prompt templates / UI / API / external call.**
- **Record/review persistence (Impl 015):** `rendering` also owns an **append-only `RenderedMessageRecord`** (auditable presentation artifact; source-domain-output ref + terminal-output kind + `VoiceMode` + validation flags preserved), an **append-only `RenderReview`** (closed 5-decision / 11-reason catalogs; display-safety only), **derived** current review status + **derived `DisplayEligibility`**, and a **`RenderedMessageRecordRepository` port + in-memory adapter** (deep-copy round-trip, mutation isolation, validated reconstitution). **Persistence is auditability, not authority**: a record is not domain truth; approval changes no domain (voice/traceability/freshness/`SupportQuality`); rejection invalidates nothing; failed attempts are auditable but never display-eligible/approvable; revision/supersession preserve the old record; **`rendering` imports no `event-recording`**, the **event catalog is not expanded**, and there is **no production DB / delivery / UI / API**.
- **Provider adapter seam (Impl 017):** `rendering` also owns a **provider seam** — `ProviderRenderingRequest` (+ the `providerRenderingRequestFrom` construction guard), an untrusted `ProviderDraft`/`ProviderDraftOutcome`, a closed **`ProviderFailure`** catalog (10), a **`ProviderAdapter`** port, a deterministic **`FakeProviderAdapter`**, and a **`requestProviderRendering`** service. It replaces **only** the draft-text step the `FakeRenderer` performs: the provider produces an **untrusted draft** that becomes a `RenderedMessage` **only** by passing the **unchanged mandatory `validateDraft`** (the `rendered` branch is the existing path; a validation failure → `provider-output-failed-validation` + the underlying `RenderingFailure[]`). The request is a **constrained projection** (no raw reasoning / chain-of-thought / mutable handle / override field); an **unsafe request is refused before the call**; **every** failure **degrades to safe non-rendering**. The provider **selects/changes no `VoiceMode`**, **creates no `TerminalOutput`/`Recommendation`/`RenderedMessage`/record**, and **persists/reviews/marks-display-eligible/delivers/emits/mutates nothing**. It imports only its own `rendering` surfaces + read-only `decision-support` *types*; **no module outside `rendering` imports it**; there is **no real SDK/API/network/`process.env`/prompt-templates** and **no `provider`/`llm` top-level module**. **A provider drafts; the validator decides; the domain stays the source of truth.**
- **Provider attempt audit (Impl 018):** `rendering` also owns a **provider-attempt audit** — `ProviderAttemptStatus` (closed; `requested`/`draft-produced` reserved), `ProviderAttemptFailureReason` (= `ProviderFailure | RenderingFailure`, real catalogs reused), a safe `ProviderDraftSummary`, a `ProviderAttemptRecord` (+ State), a **`ProviderAttemptRecordRepository` port + in-memory adapter**, and an observe-only **`auditProviderAttempt`** service. It **observes** a `ProviderRenderOutcome` (it **does not call** the provider/`requestProviderRendering`/`validateDraft`) and records an append-only **safe summary** — status + reasons + refs — with **no raw draft retention** (`rawDraftRetained` literal `false`; reconstitution rejects raw draft/text/content/prompt fields, `rawDraftRetained: true`, unknown keys, and status/flag mismatches). The record is **auditability, not authority**: it is **not** a `ProviderDraft`/source-truth/`Evidence`/`Observation`/`Understanding`/`AthleteDecision`/`DecisionSupport`/`TerminalOutput`/`RenderedMessage`/`RenderedMessageRecord`; it **creates no review/display-eligibility/delivery**, **appends no event**, **triggers no retry/reprojection/reasoning**, and **mutates nothing**; a **validation failure is not domain invalidation**. It imports only its own `rendering` surfaces + read-only `decision-support` *types*; **no module outside `rendering` imports it**; **no `event-recording` import / event-catalog expansion**; and it is **not model evaluation or telemetry**. **The audit remembers what the seam did; the draft never becomes authority.**
- **Real-provider-ready boundary (Impl 019):** `rendering` also owns a **real-provider-*ready*** async path — `ProviderSecretRef` (+ `ProviderCredentialStatus`), a non-secret `ProviderClientConfig`, a structured `ProviderInstruction` (+ `providerInstructionFrom`), `ProviderClientRequest`/`ProviderClientResponse` (+ operational `ProviderClientMetadata`), a closed `ProviderOperationalFailure` (+ `toProviderFailure`), an **async `ProviderClientBoundary`** port, a deterministic **`FakeProviderClient`**, a **`RealProviderAdapter`**, and an async **`requestRealProviderRendering`** service. It **changes only the draft source**: the synchronous Impl 017 seam is **untouched** (Option B); the async path **reuses** the unchanged `providerRenderingRequestFrom` guard + a credential fast-path + the mandatory **`validateDraft`** and returns the existing `ProviderRenderOutcome` (validation failure → `provider-output-failed-validation` + the underlying `RenderingFailure[]`). **Secret refs, never secrets** (status + opaque ref; no raw secret in records/responses/errors; no `process.env`); **structured instruction, not prompts** (derived; no template/arbitrary-prompt/chain-of-thought); **operational failures map down** to the existing `ProviderFailure` (**not expanded**); every failure **degrades to safe non-rendering** with **no automatic retry**. Provider output is **untrusted draft** (only `validateDraft` makes a message); **provider metadata is operational, not evidence**; the Impl 018 raw-free audit observes the outcome by **explicit composition** (**no automatic persistence**); and there is **no review/display-eligibility/delivery/event/domain-mutation**. It imports only its own `rendering` surfaces + read-only `decision-support` *types*; **no module outside `rendering` imports it**; **no real SDK/API/network/`process.env`/prompt-template** and **no `provider`/`llm`/`telemetry`/`evaluation` top-level module**. This is the codebase's **first `async` surface**, isolated here. **Real-provider-*ready*, not real-provider-*integrated*: a real provider changes the draft source, never the authority model.**
- **Concrete-provider adapter shell (Impl 020):** `rendering/application` also owns the **first selected-provider adapter** behind the Impl 019 async `ProviderClientBoundary` — a **`ConcreteProviderClient`** plus pure **`serializeProviderInstruction`** (+ `ConcreteProviderRequestPayload`), **`parseProviderResponse`** (+ `ConcreteProviderResponseShape`), and **`mapProviderError`** (+ `ConcreteProviderErrorKind`/`Shape`). The provider target (**OpenAI**) is recorded at the **doc/decision level (020A) only**; code stays **vendor-neutral** (`concrete-provider-*`, `providerKind: "concrete"`) so **no guard is weakened and no vendor token appears in a guarded provider file**. The client is **disabled by default** (no transport → safe `provider-unavailable`, no work; **no live call path**); a non-`present` credential fails safe before any transport; the only non-default behavior is a **deterministic in-process fixture transport for tests** (not a network call). The serializer projects **only** safe constraints (no arbitrary-prompt/chain-of-thought/secret field; no prompt template/`src/prompts`); the parser returns an **untrusted draft + operational metadata** (empty/malformed → safe operational failures; **no raw payload retained**; **no `RenderedMessage`**); the error mapper maps provider-shaped errors to the existing `ProviderOperationalFailure`, which `toProviderFailure` maps **down** to the existing `ProviderFailure` (**not expanded**; unknown → `provider-unavailable`). A draft becomes a message **only** via the unchanged `requestRealProviderRendering` → **`validateDraft`**; the raw-free audit observes the outcome by explicit composition. It imports only its own `rendering` surfaces + read-only `decision-support` *types*; **no module outside `rendering` imports it**; **no SDK/package dependency** (`package.json`/lockfile unchanged), **no network/`process.env`/raw secret/prompt template**, and **no retry/scheduler/record/review/display/delivery/event/domain side effect**. **A selected-provider *shell*, not live integration.**
- **Opt-in live-provider boundary (Impl 021):** `rendering/application` also owns the **first live-call edge** behind the same async `ProviderClientBoundary` — a **`LiveCallPolicy`** (injected value object; `disabled()`/`enabled({timeoutMs})`; disabled by default; no env inference; no global state), a **`ProviderCredentialResolver`** port (+ opaque `ProviderCredentialToken`) with a deterministic test/composition-only **`StaticProviderCredentialResolver`**, a **`LiveProviderHttpTransport`** (the **only** network-token file — native `fetch` + `AbortSignal.timeout` behind an **injected endpoint**; no hard-coded vendor URL; no retry/persist/validate/secret-in-errors), and a **`LiveProviderClient`** (implements the async `ProviderClientBoundary`; a **sibling** of `ConcreteProviderClient`). It **fails closed before any transport call** when the policy is disabled or the credential is missing/invalid; **reuses** the unchanged `serializeProviderInstruction`/`parseProviderResponse`/`mapProviderError`; **never calls `validateDraft`** (validation stays with `requestRealProviderRendering`); provider output is an **untrusted draft**; failures map onto the existing `ProviderOperationalFailure → ProviderFailure` (**not expanded**); the raw-free audit observes by explicit composition. **No SDK/package dependency** (`package.json`/lockfile unchanged), **no `process.env`/env resolver/raw secret/prompt template**, **no default/CI live call or credential**, and **no retry/scheduler/record/review/display/delivery/event/domain side effect**. The native-network guard exception is **surgical** (one approved file; vendor/SDK/env forbidden everywhere); the sync seam, `FakeProviderAdapter`, `FakeProviderClient`, and `ConcreteProviderClient` are untouched. It imports only its own `rendering` surfaces + read-only `decision-support` *types*; **no module outside `rendering` imports it**. **Live-call-*capable*, not a production rollout.**

### `delivery` *(Impl 016 — downstream exposure boundary)*
[FACT] Implements `src/modules/delivery/`: `domain/` adds ids (`DeliveryRequestId`/`DeliveryRecordId`), the closed **`DeliveryTarget`** (only `test-sink` supported), the closed **`DeliveryOutcome`** (6; `accepted-for-delivery` reserved), the closed **`DeliveryFailureReason`** (10; `provider-unavailable` excluded, `sink-unavailable` added), `DeliveryRequest`, `DeliveryEligibilityCheck` (+ `primaryFailureReasonFor`), the `DeliverySink` interface + deterministic **`InMemoryTestSink`**, and the auditable `DeliveryRecord` (+ `DeliveryRecordState`); `application/` adds the **`DeliveryRecordRepository` port** + `InMemoryDeliveryRecordRepository` + the **`requestDelivery`** service. Imports **only `shared-kernel` + read-only `rendering`** types/functions.
- **Clarify:** delivery is **downstream exposure, never rendering and never domain** — `rendering` owns display eligibility; delivery only **attempts to expose** an already-eligible `RenderedMessageRecord` and records the attempt. It **verifies** eligibility by calling `displayEligibilityOf(record)` (**never** re-derives it) and calls the **deterministic test sink only** when the record is eligible *and* the target is the supported `test-sink`; not-reviewed/rejected/superseded/failed-render/missing-ref records and unsupported/reserved targets are **blocked** without calling the sink (raw rendering reasons retained, mapped to a closed failure reason). A `DeliveryRecord` is **not** domain authority (`≠ Evidence/Observation/Understanding/DecisionSupport/AthleteDecision`); **delivery success is not evidence**, **delivery failure is not domain invalidation**; delivery **mutates no rendered record/aggregate**, **emits no event**, and **triggers no reasoning/reprojection/retry**. The audit repo uses the Impl 010 pattern (deep-copy round-trip; mutation isolation; validated reconstitution). `delivery` imports **no `event-recording`**, the **event catalog is not expanded**, **`rendering` does not import `delivery`**, and **no upstream module imports `delivery`**. **No real provider/channel / UI / API / scheduler / retry / event bus / production DB.**

---

## 4. Structural Guarantees

[FACT] What is **impossible to construct today**, with the defense that makes it so. (These are enforced by module surface design + the negative-capability and boundary tests, not by convention.)

| # | Made impossible | Defense (module / test) |
|---|---|---|
| 1 | Raw `Observation` becoming meaning directly | `Observation` has no meaning field; meaning requires `contextualize` + `SignalDetectionPolicy`; `observation` cannot import `reasoning` (boundary test) |
| 2 | `Signal` becoming `Evidence` without a `Hypothesis` | `createEvidenceCase` not exported; `EvidenceCase` only via `Hypothesis.attachEvidence` (reasoning negative tests) |
| 3 | An `EvidenceCase` existing standalone | same as #2 — no constructor on the public surface |
| 4 | A `Hypothesis` becoming certainty | no "certain/proven" lifecycle state; confidence always calibrated/defeasible (reasoning lifecycle tests) |
| 5 | Understanding promoting from repetition alone | promotion gated on survived challenge across ≥2 distinct conditions in `update-policy` (understanding tests) |
| 6 | Claim confidence mapping to understanding | `reasoningOutcomeFrom` adapter drops `ClaimConfidence` (understanding adapter test) |
| 7 | `SafeVoiceCeiling` becoming a `VoiceMode` | distinct types in distinct modules; `maxVoiceForCeiling` maps ceiling→voice in `decision-support` (negative test) |
| 8 | Claim confidence selecting voice | `VoiceSelectionInputs` has no confidence field (decision-support negative test) |
| 9 | `Recommendation` without complete traceability + confident ceiling | final guard in `VoiceSelectionPolicy`; degrades to Framing otherwise (decision-support + e2e tests) |
| 10 | Risk escalating toward `Recommendation` | `RiskGate` yields only `caution-warning`; policy routes risk to `Warning`, never up (decision-support negative test) |
| 11 | `Inquiry` being a `VoiceMode` | `Inquiry` has no `voice` field; separate output type (decision-support negative test) |
| 12 | `DecisionSupportCase` owning `AthleteDecision` | only an `AthleteDecisionRef`; asserted `undefined` in the e2e proof (gates-and-quality + e2e tests) |
| 13 | Upstream depending on downstream | dependency-boundary tests per module + the e2e structural guard that no production file imports all four surfaces |
| 14 | `athlete` reaching a downstream module | `athlete` imports only `shared-kernel`; `athlete-boundary` test asserts no `observation`/`reasoning`/`understanding`/`decision-support` import (Impl 007) |
| 15 | Purpose history being overwritten / a purpose change rewriting the past | `PurposeHistory` append-only; `Athlete` immutable-by-operation; `PurposeChanged` carries no hypothesis mutation; never mutates `UnderstandingProfile` (Impl 007 tests) |
| 16 | Revealed behavior becoming declared purpose / inferred purpose | only an athlete-sourced declaration creates a version; runtime guard rejects non-purpose/non-athlete-sourced values; `RevealedPurposeSignal` has no change path (Impl 007 declared-vs-revealed test) |
| 17 | A non-current projection raising the voice | freshness only clamps the ceiling down; `clampCeilingByFreshness` never raises; `invalid`/`unknown` → `none` (Impl 008 freshness tests) |
| 18 | A stale/invalid projection earning a `Recommendation` | `Recommendation` needs `confident`; non-current freshness caps below it (→ `Withholding` for invalid/unknown); integration test |
| 19 | Refresh mutating an old projection / inventing traceability | `applyFreshness` returns a new frozen assessment; the old stays auditable; source refs derive only from the real trace (Impl 008 tests) |
| 20 | A trigger globally staling unrelated projections | `projectionRefreshPolicy` is selective by source-ref intersection; non-matching → `keep-current` (Impl 008 refresh-policy tests) |
| 21 | `DecisionSupportCase` owning the athlete's decision | the case holds only an `AthleteDecisionRef`; the `AthleteDecision` lives in `athlete`; integration test asserts no decision object on the case (Impl 009) |
| 22 | An `AthleteDecision` becoming `Evidence`/`Understanding` directly | the only re-entry is `decisionAsObservation` → `SubjectiveObservation`; no decision→Evidence/Understanding adapter exists; the ladder is intact (Impl 009 tests) |
| 23 | Obedience/compliance scoring of a decision | `AthleteDecision` carries no compliance/obedience/noncompliance/shame/reward field; `divergedFromSupport` is neutral; following ≠ success, not-following ≠ failure (Impl 009 negative tests) |
| 24 | An outcome retroactively grading the support | `DecisionOutcomeRef` is separate; recording a (divergent) decision leaves `SupportQuality` unchanged — integrity-at-the-time (Impl 009 test) |
| 25 | Reconstituting an invalid persisted object | `reconstitute` validates and **throws** on invalid state (no falsifier, empty occasion, missing athleteRef, out-of-order purpose history, orphan amendment); never a raw field bag (Impl 010 negative tests) |
| 26 | A repository handing out a live internal reference | adapters store deep-copied `toState()` and load via `reconstitute` (deep-copied); two finds are independent; mutating a load never touches the store (Impl 010 mutation-isolation tests) |
| 27 | Persistence silently dropping history/traceability/freshness | round-trip preserves append-only history, supersession, traceability refs, and (via helper) projection freshness (Impl 010 round-trip tests) |
| 28 | A production persistence layer / infrastructure leaking in | no `src/infrastructure`, no `persistence`/`repositories` module, no DB/ORM/event-bus token in production code (Impl 010 persistence-boundary guard) |
| 29 | An event record carrying copied aggregate state / an arbitrary payload bag | `EventPayloadRef` has only `kind`/`id`/`role?`/`ownerModule?`; no `payload`/`data`/`metadata: unknown`; reconstitution rejects extra keys (Impl 011 ref-only tests) |
| 30 | An event record acting as a command / executing or mutating downstream | a `DomainEventRecord` has no behavior beyond being recorded; appending mutates nothing; causation/correlation are lineage/grouping, not triggers (Impl 011 negative tests) |
| 31 | Overwriting/deleting event history; an event record bypassing the closed catalog | `DomainEventRecordLog`/repository are append-only (duplicate ids rejected; no update/delete); `record()`/`reconstitute()` validate type↔module/category + required refs against the closed catalog (Impl 011 append-only + validation tests) |
| 32 | `event-recording` coupling to domain modules / the kernel becoming event-aware / a bus or event sourcing | `event-recording` imports only `shared-kernel`; no domain module imports it; the catalog lives outside `shared-kernel`; no bus/publish/subscribe/handler/event-sourcing/serialization token or dir exists (Impl 011 boundary + negative-capability guards) |
| 33 | A reprojection run mutating an aggregate / calling a write method | `check-only` is the only implemented mode; the harness calls only reads; before/after deep-equality snapshot is unchanged and a throwing `save` is never reached (Impl 012 mutation-safety tests) |
| 34 | Rebuilding aggregate state from the event log / replaying events as commands | an event referencing an absent aggregate yields `event-record-only`/`missing-source`, never synthesized state; candidate detection is a pure events→targets map that executes nothing (Impl 012 empty-repo + candidate tests) |
| 35 | A reprojection run promoting freshness / strengthening voice / overwriting a stored view / emitting an output | recompute may only re-derive the same-or-more-cautious view (stale→ceiling lowered, never raised); drift is reported (`changed`), never overwritten; no `TerminalOutput`/`SupportQuality` rewrite/`Purpose` overwrite/`DomainEventRecord` is produced (Impl 012 drift + ceiling + negative tests) |
| 36 | A production `reprojection` module / scheduler / event sourcing / projection repository leaking in | the harness lives under `__tests__/`; structural guard asserts no `src/modules/{reprojection,projection,scheduler,event-sourcing}` and no scheduler/bus/DB/serialization token; no domain module imports the harness (Impl 012 structural guards) |
| 37 | Manual input becoming meaning / a `Signal` / inferred state at ingestion | the adapter's only output is an `ObservationSet` built via `recordObservationSet`; it never calls `detectSignals`; subjective words are verbatim; no readiness/fatigue/impact/capacity field; negative tests assert no Signal/Evidence/Hypothesis/Understanding/DecisionSupport (Impl 013) |
| 38 | The adapter inventing a value / silently interpreting ambiguity / losing rejected input | missing data is an explicit `missing-data` observation (never invented); ambiguity → a limitation or rejection; rejection saves nothing (a throwing `save` is never reached) (Impl 013 partial/rejection tests) |
| 39 | `observation` coupling to `event-recording` or a downstream module / the adapter mutating athlete records | the adapter imports only `observation` + `shared-kernel`; structural guard asserts `observation` imports no `event-recording`/`reasoning`/`understanding`/`decision-support`/`athlete`; an athlete-decision report is observation-only (no `AthleteDecisionRecord` write); the optional `ObservationSetRecorded` is harness-composed, ref-only (Impl 013 boundary + neutral-integration tests) |
| 40 | A renderer selecting/escalating voice or creating a `TerminalOutput`/`Recommendation` | rendering reads a read-only projection; voice may only match/soften; the validator rejects recommendation language unless the domain voice is `Recommendation` (`voice-escalation`/`recommendation-created-by-renderer`); a style request (`"be decisive"`) is not a `SAFE_STYLE` → `unsupported-style-request` (Impl 014 validator tests) |
| 41 | Generated text hiding uncertainty / inventing a fact or citation / becoming domain authority | the mandatory validator rejects `uncertainty-hidden`/`limitation-hidden`/`invented-fact`/`missing-traceability`; `Inquiry` stays a question, `Withholding` stays a refusal; a `RenderedMessage` carries no aggregate-write path and is not re-ingested as `Evidence`/`Observation`/`Understanding`/`AthleteDecision` (Impl 014 validator + negative tests) |
| 42 | A real LLM provider / `rendering` coupling / domain mutation by rendering | `rendering` imports only `shared-kernel` + read-only `decision-support` *types*; no domain module imports it; structural guard asserts no `src/{llm,api,ui,infrastructure}` / `src/modules/{llm,openai,provider}` and no provider/network token; the fake renderer is deterministic; rendering mutates/emits nothing (Impl 014 structural + neutral tests) |
| 43 | A persisted/approved rendered message becoming domain authority | a `RenderedMessageRecord` carries no domain field/write handle; approval changes no `VoiceMode`/traceability/freshness/`SupportQuality`; rejection invalidates nothing; the record is not usable as `Evidence`/`Observation`/`Understanding`/`DecisionSupport`/`AthleteDecision` (Impl 015 record + negative tests) |
| 44 | A failed render displayed / review history overwritten / old record lost | a failed attempt is never display-eligible and cannot be approved; review history is append-only with derived status; revision/supersession preserve the old (immutable) record; display eligibility is derived, never stored authority, never delivery (Impl 015 record/eligibility tests) |
| 45 | Rendered-message persistence emitting an event / leaking to a DB or delivery | the record repo is in-memory (port + adapter; deep-copy mutation isolation; validated reconstitution); `rendering` imports no `event-recording`; the event catalog has no `RenderedMessageRecorded`/`RenderReviewed`; structural guard asserts no DB/delivery/provider/UI/API (Impl 015 persistence + negative-capability tests) |
| 46 | Delivering a non-display-eligible record / calling the sink on a blocked request | `requestDelivery` gates on `displayEligibilityOf(record)`; not-reviewed/rejected/superseded/failed-render/missing-ref records and unsupported/reserved targets yield `blocked-not-eligible`/`not-attempted` and the sink is provably never called (Impl 016 boundary tests assert `sink.delivered.length === 0`) |
| 47 | Delivery re-deriving eligibility / success validating or failure invalidating the domain | delivery reuses rendering's `displayEligibilityOf` and only maps its raw reasons (no parallel rule set); delivery success/failure/cancellation leave the rendered record and domain output byte-for-byte unchanged; a `DeliveryRecord`/`DeliveryOutcome` carries no domain field and is not usable as `Evidence`/`Observation`/`Understanding`/`AthleteDecision`/`SupportQuality` (Impl 016 no-mutation + negative tests) |
| 48 | Delivery emitting an event / a real provider/channel / `delivery` coupling / upstream importing `delivery` | `delivery` imports only `shared-kernel` + read-only `rendering`; the repo-named files import only own module + `shared-kernel`; structural guard asserts no `event-recording` import, no `Delivery*` event-catalog entry, no `src/{api,ui,infrastructure}` / `src/modules/{provider,channel,notification,scheduler,event-bus,llm}`; `rendering` does not import `delivery` and no upstream module does (Impl 016 boundary + negative-capability tests) |
| 49 | A provider draft becoming a `RenderedMessage` without passing the validator / the provider returning a message directly | `requestProviderRendering` only builds a `RenderedMessage` via `validateDraft`; the `ProviderAdapter` port returns `ProviderDraftOutcome` (draft/failure), never a message; a `ProviderDraft` has only `text`/`providerKind`/`warnings` (no preservation flags it sets itself) (Impl 017 service + adapter-boundary tests) |
| 50 | A provider draft escalating voice / inventing a fact / hiding uncertainty/limitations / answering an inquiry / advising a withholding | each unsafe fake scenario is refused by the **unchanged** `validateDraft` (`voice-escalation`/`invented-fact`/`uncertainty-hidden`/`limitation-hidden`/`inquiry-rendered-as-answer`/`withholding-rendered-as-advice`), surfaced as `provider-output-failed-validation`; an unsafe request is refused before the call; every failure degrades to safe non-rendering (Impl 017 service tests) |
| 51 | A real provider SDK/network / prompt-as-code / `provider` module / provider-seam coupling or side effect | provider files import only own `rendering` surfaces + read-only `decision-support` *types*; structural guard asserts no `openai`/`anthropic`/`axios`/`node:http(s)`/`fetch(`/`http(s)://`/`process.env` token, no `src/{providers,prompts,api,ui,infrastructure}` / `src/modules/{provider,llm,openai,anthropic,model}`; provider request exposes no raw reasoning/chain-of-thought/handle; the seam persists/reviews/delivers/emits/mutates nothing, and no module outside `rendering` imports it (Impl 017 negative-capability tests) |
| 52 | A provider-attempt audit retaining a raw draft / unsafe text / prompt / chain-of-thought | `ProviderDraftSummary` has no `draft`/`text`/`content`/`raw`/`excerpt`/`prompt` field; `rawDraftRetained` is the literal `false`; `reconstitute` rejects `rawDraftRetained: true` and any smuggled raw-content/payload-bag key; content tests assert no raw draft text in the record state (Impl 018 record + negative tests) |
| 53 | A provider-attempt audit calling the provider/validator, becoming authority, or firing a downstream effect | `auditProviderAttempt` is a pure mapping of an observed `ProviderRenderOutcome` — structural guard asserts it does not call `requestProviderRendering`/`ProviderAdapter.draft`/`validateDraft`; the record is not `Evidence`/`Observation`/`Understanding`/`AthleteDecision`/`DecisionSupport`/`RenderedMessage`/`RenderedMessageRecord`, creates no review/display/delivery, appends no event, and a validation failure does not invalidate the domain (Impl 018 audit + negative tests) |
| 54 | A provider-attempt event / `provider-audit`/`telemetry`/`evaluation` module / audit coupling or upstream import | the audit imports only own `rendering` surfaces + read-only `decision-support` *types*; structural guard asserts no `event-recording` import, no `ProviderAttempt*` event-catalog entry, no `src/modules/{provider-audit,telemetry,evaluation,provider,llm,…}`, and no module outside `rendering` imports the audit; reasons reuse the real `ProviderFailure` + `RenderingFailure` catalogs (no invented parallel catalog) (Impl 018 boundary + negative-capability tests) |
| 55 | A real-provider draft bypassing the validator / the real-ready path changing the sync seam | `requestRealProviderRendering` reuses `providerRenderingRequestFrom` + the unchanged `validateDraft` and returns the existing `ProviderRenderOutcome`; the `RealProviderAdapter` returns only a `ProviderDraftOutcome` (never a message); a structural test asserts the synchronous `ProviderAdapter` port stays sync (no `Promise`) and the Impl 017 seam/tests are unchanged (Impl 019 boundary + sync-seam tests) |
| 56 | A real provider SDK/network/secret/prompt leaking in / a raw secret persisted-logged-errored | real-provider files import only own `rendering` surfaces + read-only `decision-support` *types*; structural guard asserts no `openai`/`anthropic`/`axios`/`node:http(s)`/`fetch(`/`process.env` token, no `src/{providers,prompts,…}` / `src/modules/{provider,llm,telemetry,evaluation,…}`; `ProviderSecretRef` is a status + opaque ref (no field for a raw secret); a returned outcome leaks no `secret`/`apikey`/`credential`/ref (Impl 019 negative-capability tests) |
| 57 | A real provider failure retrying/escalating / mapping that expands `ProviderFailure` / metadata becoming evidence | `ProviderOperationalFailure` maps **down** to the existing closed `ProviderFailure` (`PROVIDER_FAILURES.length === 10`, not expanded); the client is called at most once (no automatic retry); a non-`present` credential fails before any client call; provider metadata is operational (the outcome surfaces no `metadata`/`latency`/`finishReason` as a domain field) (Impl 019 failure-mapping + boundary tests) |
| 58 | A concrete provider being used without an explicit selection / a live call running by default / a vendor token leaking into a guarded provider file | the provider target is recorded **doc-level** in 020A (a selection test asserts the decision is explicit); `ConcreteProviderClient` is **disabled by default** (no transport → safe `provider-unavailable`, no I/O — there is no live-call code); the negative-capability guard asserts no `openai`/`anthropic`/`axios`/`node:http(s)`/`fetch(`/`http(s)://`/`process.env` token in the `concrete-provider-*` files and code stays vendor-neutral (Impl 020 selection + negative-capability tests) |
| 59 | The concrete serializer carrying an arbitrary prompt/chain-of-thought / the parser retaining a raw payload or creating a message / provider metadata becoming evidence | `serializeProviderInstruction` projects **only** safe constraints (the payload has no prompt/system/chainOfThought/secret field — a key-set test asserts it); `parseProviderResponse` returns an **untrusted draft + operational metadata only** (empty/malformed → safe failures; content tests assert no raw payload survives; it never builds a `RenderedMessage`); a draft becomes a message only via the unchanged `validateDraft` (Impl 020 serialization + parsing tests) |
| 60 | The concrete error mapper expanding the failure catalog / a missing/unknown error not failing safe / an SDK dependency leaking in | `mapProviderError` maps provider-shaped errors to the existing `ProviderOperationalFailure`, which `toProviderFailure` maps **down** to the existing `ProviderFailure` (**not expanded**); missing/invalid credential and unknown errors fail safe (→ `provider-unavailable`) leaking no payload/secret; a package guard asserts **no runtime dependency** and devDeps remain `typescript` + `@types/node` (Impl 020 error-mapping + package-guard tests) |
| 61 | A live call running by default / a disabled-or-uncredentialed call reaching the network / the default suite or CI making a live call | `LiveCallPolicy.disabled()` is the default; `LiveProviderClient` **fails closed before any transport call** when the policy is disabled or the credential is missing/invalid — a spy transport proves it is never invoked; the default suite drives only a fake in-process transport and requires no credential (Impl 021 live-call-gate tests) |
| 62 | A native network token / SDK dependency / env read / vendor token leaking past the surgical exception | the network token (`fetch(`/`https?://`/`node:http(s)`) is allowed in **exactly one** approved file (`live-provider-http-transport.ts`) — the Impl 014 + Impl 017 guards assert it is the *only* network file and forbid `process.env`/`openai`/`anthropic`/`axios` **everywhere**, including that file; a package guard asserts **no runtime dependency** (Impl 021 negative-capability + rendering/provider guards) |
| 63 | A live draft bypassing the validator / a transport failure expanding the catalog or invalidating the domain / metadata becoming evidence | `LiveProviderClient` returns only a `ProviderClientResponse` and **never calls `validateDraft`** — a draft becomes a message only via the unchanged `requestRealProviderRendering` → `validateDraft`; transport conditions map onto the existing `ProviderOperationalFailure → ProviderFailure` (**not expanded**; unknown → safe `provider-unavailable`); the outcome surfaces no operational metadata as a domain field and leaks no credential (Impl 021 gate + failure-mapping tests) |

---

## 5. End-to-End Reflection Proof (Implementation 006)

[FACT] The integration test (`src/modules/__tests__/end-to-end-responsible-reflection.test.ts`) runs a synthetic scenario ("I felt unusually heavy": an elevated HR measurement + a subjective report + a missing power datum) through all four modules and asserts the terminal output is `DecisionSupport` with `VoiceMode = Reflection`.

[FACT] **Why the first full output is `Reflection` — by the modules' own logic, not by force:**
1. A single **`supported`** outcome is first contact: `applyOutcome` sets `level = higherOf("Unknown","Working") = Working`; with one distinct condition (`< 2`) it does not reach `Trusted`/`Mature`.
2. `deriveSafeVoiceCeiling(Working, low fragility, fresh)` = **`tentative`**.
3. `maxVoiceForCeiling("tentative")` = **`Reflection`**.
4. **Complete traceability is not enough for Recommendation** — the Recommendation guard also requires a `confident` ceiling.
5. **Clean gates do not override the understanding ceiling** — all five gates pass, yet the ceiling still caps the voice at Reflection.

[FACT] The proof also asserts the chain's honesty: the quality limitation survives as an auditable `SignalRejection`; traceability resolves back to the `ObservationSet`; agency is preserved; and no `AthleteDecision` is owned.

> [ASSUMPTION] **Aurora can complete the reasoning chain and still refuse to overreach.**

---

## 6. What Does Not Exist Yet

[FACT] Aurora does **not** yet have, and this is by design at this stage:

| Absent | Intentional? | Note |
|---|---|---|
| **Full** `athlete` aggregate | **Intentional** | A thin **Purpose-first** `athlete` module now exists (Impl 007); state/capacity/constraints/path-memory/identity-detail/reports are still absent. |
| Real `Purpose` source | **Implemented (Impl 007)** | Now a real, athlete-owned, versioned, append-only source; reaches `decision-support` as `PurposeContext` via a neutral adapter. The decision-support `PurposeContext` *placeholder* is **partly retired** (§7). |
| Real `Constraints` source | **Intentional** | Belongs to a future `athlete` slice (not in the Purpose-first slice). |
| Inferred athlete state / capacity / readiness / fatigue | **Intentional** | Never implemented; `athlete` owns the *given*, never the *inferred*. |
| Real `RiskAssessment` source | **Intentional** | Provided as input placeholder; no diagnostic engine. |
| **Manual input boundary (real "data in")** | **Implemented (Impl 013)** | An `observation`-owned Manual Input Adapter records manual input faithfully as an `ObservationSet` (provenance/source/quality preserved; verbatim words; explicit missing data; rejection/partial outcomes), persisted via the repository port — no interpretation. |
| UI / API / external ingestion entrypoint | **Intentional** | The adapter is an in-process boundary; how input is *collected* (UI) or *submitted* (API), auth, and external/FIT/wearable import remain future. |
| Garmin/FIT importer | **Intentional** | First real input is the **manual adapter** (Impl 013); device/FIT adapters are a later spec, behind the same `ObservationSet` entry point. |
| **Repository ports + in-memory persistence** | **Implemented (Impl 010)** | Validated `toState()`/`reconstitute()` + ports + in-memory adapters for the six boundaries; round-trip preserves invariants/traceability/freshness/history. |
| Production DB / ORM / schema / migrations | **Intentional** | Persistence is **ports + in-memory only**; no technology chosen. The domain's `toState()` drives the (future) store, never the reverse. |
| Cache | **Intentional** | Projections recompute; no cache layer. |
| Projection **freshness** on `UnderstandingAssessment` | **Implemented (Impl 008)** | Explicit `current`/`stale`/`partial`/`invalid`/`unknown` + `derivedAt` + source refs; non-current only lowers the voice (invalid/unknown → ceiling `none`), via the existing ceiling. |
| Generic projection **engine** / top-level `projection` module | **Intentional** | Freshness is local to `understanding` for the one concrete projection; a shared kernel/engine waits for a second projection (`ImpactAssessment`). |
| `ImpactAssessment` (second projection) | **Intentional** | Not introduced; Spec 008 governs *how any projection behaves*, not which exist. |
| **Domain event/outcome records + traceability envelope** | **Implemented (Impl 011)** | The `event-recording` module records *what happened* — append-only, ref-only `DomainEventRecord` (occurrence/outcome) over a closed catalog, with a `TraceabilityEnvelope` + in-memory log/repository. Complements aggregate repositories; does not replace them. |
| Event bus / publish-subscribe / handlers / async delivery | **Intentional** | Records are stored, never delivered or executed. `PurposeChanged`/refresh triggers and event records are values, not bus messages; there is no runtime bus, subscriber, or handler. |
| Event sourcing / production event store / serialization format | **Intentional** | The log records occurrences; aggregates rebuild via `reconstitute`, not by replaying the log. No event-sourcing rebuild path, production store, or serialization tech is chosen. |
| **Reprojection (recompute-and-compare)** | **Implemented (Impl 012)** | A **neutral, check-only** harness (`__tests__/reprojection-harness/`, **not a production module**) recomputes `UnderstandingAssessment`, recalculates freshness, detects candidates from event records, and reports drift/findings — mutating nothing. |
| Production `reprojection` module / scheduler / projection repository / production service | **Intentional** | Reprojection is proven as a check-only harness; a production recompute *service*, a scheduler/background refresh, a projection store, and an orchestration layer are deferred (each behind the same contracts when specified). |
| UI | **Intentional** | No rendering; outputs are domain values, not copy. |
| API | **Intentional** | No external entrypoint boundary specified yet. |
| **Rendering boundary (output-out)** | **Implemented (Impl 014)** | A deterministic `rendering` module turns a domain-approved `TerminalOutput` into human-facing text via a fake renderer + mandatory validator — voice/uncertainty/limitations/freshness/traceability/agency preserved; generated text is presentation, not authority. |
| **Provider adapter seam (output-out)** | **Implemented (Impl 017)** | Inside `rendering`: a deterministic **fake provider** drafts text behind the **unchanged mandatory `validateDraft`**; an untrusted draft becomes a `RenderedMessage` only on validation; failures degrade to safe non-rendering — the validator, not the provider, is the authority. |
| **Real-provider-ready boundary (output-out)** | **Implemented (Impl 019)** | Inside `rendering`: an **additive async** `ProviderClientBoundary` (deterministic `FakeProviderClient`) makes a real provider **pluggable** — operational secret refs, structured instructions, operational-failure mapping — behind the **unchanged `validateDraft`**; the sync seam is untouched. Real-provider-**ready**, not integrated. |
| **Concrete-provider adapter shell (output-out)** | **Implemented (Impl 020)** | Inside `rendering/application`: the **first selected-provider adapter** behind the async `ProviderClientBoundary` — a **disabled-by-default `ConcreteProviderClient`** + pure serializer/parser/error-mapper. Provider target (**OpenAI**) selected **doc-level (020A) only**; code stays vendor-neutral (`concrete-provider-*`). Untrusted draft → unchanged `validateDraft`; operational failures map down without expanding `ProviderFailure`; **no SDK/network/live-call/secret/prompt**. A selected-provider **shell**, not live integration. |
| **Opt-in live-provider boundary** | **Implemented (Impl 021)** | Inside `rendering/application`: a **`LiveProviderClient`** behind the async `ProviderClientBoundary`, an injected **`LiveCallPolicy`** (disabled by default; explicit opt-in), an injected **`ProviderCredentialResolver`** (deterministic `StaticProviderCredentialResolver`; no env), and a single **`LiveProviderHttpTransport`** (native `fetch`, the only network file). Fails closed before transport; reuses serializer/parser/error-mapper; `validateDraft` stays mandatory; audit raw-free. **Live-call-*capable*, not a production rollout.** |
| Production live-call rollout (live call on, real credentials/endpoint) | **Intentional** | The live boundary is *capable* but **disabled by default** with **no env/secret resolver** and **no CI/default-suite live call**; turning it on in production (a real endpoint + secret mechanism) is a **later slice**. |
| Real LLM provider SDK / production secret-env mechanism / prompt templates | **Intentional** | The provider boundary is proven with a **deterministic fake provider/client** (Impl 017/019), a **vendor-neutral concrete shell** (Impl 020), and an **opt-in live boundary using native `fetch`** (Impl 021, disabled by default); a real *deployed* client supplies a real endpoint/credential through the injected seams (secrets/network isolated in the one approved transport + a future env/secret resolver) behind the same validator; **no SDK/installed dependency, no `process.env`/env resolver/real secret-mechanism, no prompt-templates-as-code, and no `provider`/`llm` module** exist. |
| **Provider-attempt audit (output-out safety)** | **Implemented (Impl 018)** | Inside `rendering`: an append-only **`ProviderAttemptRecord`** (port + in-memory adapter) records *what the seam did* by observing a `ProviderRenderOutcome` — safe summary (status + real-catalog reasons), **no raw draft retention** — auditability not authority; observe-only (calls no provider/validator); creates no record/review/display/delivery/event. |
| Provider-attempt / provider event records | **Intentional** | A `ProviderAttemptRecord` is **not** an event record; the audit imports no `event-recording` and the catalog is not expanded; ref-only provider events remain future. |
| Raw provider draft retention | **Intentional (forbidden)** | Drafts are untrusted; `rawDraftRetained` is the literal `false`; raw draft/text/content/prompt fields are rejected on reconstitution — debugging is reason-based, not text-based (Impl 018). |
| Provider telemetry / model evaluation | **Intentional** | The provider-attempt audit is auditability/safety-debugging only; it grades no model/recommendation quality and infers no athlete state. |
| **Rendered-message record / review persistence** | **Implemented (Impl 015)** | Inside `rendering`: an append-only `RenderedMessageRecord` + `RenderReview` (display-safety) + derived `DisplayEligibility`, behind a repository port + in-memory adapter — auditability, not authority. |
| Rendered-output / delivery **event records** | **Intentional** | A `RenderedMessageRecord`/`DeliveryRecord` is **not** an event record; `rendering`/`delivery` import no `event-recording`; the catalog is not expanded (ref-only `RenderedMessageRecorded`/`DeliveryAttempted`, if ever). |
| **Delivery / exposure boundary** | **Implemented (Impl 016)** | A downstream `delivery` module exposes a *display-eligible* `RenderedMessageRecord` to a deterministic **test sink** and records an auditable `DeliveryRecord` (port + in-memory adapter). Verifies (never re-derives) eligibility; blocks everything ineligible; success/failure never touch the domain. |
| Real delivery **channel / provider** (email/SMS/push/web) | **Intentional** | The delivery boundary is proven with a **deterministic `InMemoryTestSink`**; a real channel would implement the same `DeliverySink` interface behind the same eligibility gate. |
| Scheduler / retry / background-job layer | **Intentional** | `requestDelivery` is synchronous and deterministic; retries/cancellation lifecycles and any `accepted-for-delivery` two-phase flow are deferred. |
| Rendered-message / delivery **production persistence** | **Intentional** | The record/audit repositories are **in-memory** (port + adapter); no production DB/schema/ORM/migration. |
| UI / API for reviewed/delivered messages | **Intentional** | Display eligibility is **derived, not delivery**; delivery exposes only to a test sink; how an approved message reaches a human surface (UI/API/notification) is future. |
| Notification layer | **Intentional** | A real channel/notification transport is out of scope; only a test/manual/future-channel abstraction exists. |
| `AthleteDecision` feedback loop | **Implemented (Impl 009)** | Athlete-owned, append-only `AthleteDecision` in `athlete`; referenced (not owned) by the case; re-enters as `SubjectiveObservation`. The `AthleteDecisionRef` placeholder is **retired**. |
| Full `DecisionOutcome` object / pattern engine | **Intentional** | Only `DecisionOutcomeRef` exists; outcome modeling and decision-pattern→hypothesis inference are deferred. |
| Compliance / obedience / outcome-based validation | **Intentional (forbidden)** | No score is produced; the outcome never grades `SupportQuality`. |
| Training-plan generator | **Intentional** | Explicitly forbidden at this stage. |
| Real-world ingestion adapter | **Intentional** | Synthetic fixture only. |

[ASSUMPTION] None of the above is a failure. Each was excluded so the core's invariants could be proven *before* the surfaces most likely to erode them are introduced.

---

## 7. Known Placeholders / Adapters

[FACT] Current placeholders and adapters, each with why it exists, what it protects, and its eventual replacement.

| Placeholder / adapter | Why it exists | What it protects | Future replacement |
|---|---|---|---|
| `ReasoningOutcome` adapter (`reasoningOutcomeFrom`, local to `understanding`) | Lets understanding consume reasoning **outcomes** without importing reasoning internals (type-only import) | Keeps claim confidence + raw evidence out of understanding; "understanding earned from tested outcomes" | Stable cross-module outcome contract / event when an event surface lands |
| `PurposeContext` placeholder (input to `decision-support`) | **Partly retired (Impl 007).** A real, versioned purpose now exists in `athlete`; the decision-support input is still a passed-in `PurposeContext` value (no `decision-support`→`athlete` import) | Lets `PurposeGate` run without `decision-support` importing/owning purpose | A production coordinator that snapshots current purpose into `PurposeContext` (the harness adapter is the current seam) |
| `RiskAssessment` placeholder (input to `decision-support`) | No diagnostic/risk engine yet | Lets `RiskGate` run (caution-only) without inventing risk inside the case | Real risk assessment service, still caution-only |
| `AthleteDecisionRef` (field on `DecisionSupportCase`) | **Retired as a placeholder (Impl 009).** It now references a real, athlete-owned `AthleteDecision` in `athlete` | Keeps the decision **referenced, never owned**; `SupportQuality` ≠ outcome | — (the loop is built; production persistence/event surface is the remaining piece) |
| `DecisionOutcomeRef` (reference only, Impl 009) | The outcome is later/separate; a full `DecisionOutcome` object is deferred | Keeps outcome distinct from the decision; outcome never grades support | A specialized `DecisionOutcome`/observation type when outcome modeling is specified |
| Decision harness adapters (`src/modules/__tests__/decision-observation-adapter.ts`) | Converting `AthleteDecision` → `SubjectiveObservation` / adapting the ref needs `observation`/`decision-support`; `athlete` must not import them | Keeps `athlete` a pure upstream leaf; re-entry is observation-only (Impl 006 precedent) | A production application service when a real entrypoint is specified |
| Purpose harness adapters (`src/modules/__tests__/purpose-adapters.ts`) | Converting purpose → `PurposeContext` / applying `PurposeChanged` → `markUnderstandingStale` requires importing downstream; `athlete` must not | Keeps `athlete` a pure upstream leaf; coordination lives outside it (Impl 006 precedent) | A production application service when a real entrypoint is specified |
| `ProjectionRefreshPolicy` applied via harness/caller (Impl 008) | Refresh has no scheduler/event surface yet; the policy is a pure decision | Keeps refresh deterministic and selective without infrastructure; `understanding` owns freshness, the caller applies triggers | An event-driven/maintained refresh when the event surface + persistence land |
| `PurposeReinterpretationStatus` / result (type only) | The reinterpretation *engine* is deferred | Gives a stable shape for future reinterpretation without deciding statuses now | A reinterpretation pipeline once reasoning is purpose-version-aware |
| In-memory repositories (Impl 010, module-local `application/`) | Persistence proven without a DB; the in-memory adapter is the test double for each port | Proves round-trip safety + invariant preservation with no technology committed | A real adapter behind the **same port** when a DB/persistence requirement is specified |
| `<Aggregate>State` types + `reconstitute()` (Impl 010) | Validated rebirth path (constructors/`toProps()` are private); persistence shape is an adapter contract | Keeps rehydration validating, never a raw field bag; keeps state out of the primary public domain API | Possibly a versioned `<Aggregate>State` (`schemaVersion`) at the first real serialization |
| Synthetic end-to-end fixture (`__tests__`) | The e2e composition proof must be deterministic | Proves composition + restraint without committing to an input format | **Partly retired (Impl 013):** a real **manual** ingestion boundary now exists; the synthetic fixture remains the deterministic e2e driver until a real entrypoint is specified |
| Manual Input Adapter (Impl 013, `observation/application/manual-input-*`) | First real "data in" must be proven *before* any UI/API/parser is built on top | Keeps ingestion a faithful scribe — records source material, never meaning; persists via the port; imports no downstream module or `event-recording` | A production entrypoint (UI/API) and external (FIT/wearable) adapters that submit to the same `ObservationSet` boundary |
| Deterministic fake renderer (Impl 014, `rendering/domain/fake-renderer.ts`) | The rendering *boundary contract* must be proven *before* any real provider; tests must be deterministic | Proves voice/uncertainty/traceability/agency preservation with no provider/nondeterminism; the **validator** is the provider-independent guarantee a real renderer must also pass | A real LLM provider adapter **behind the same validator**, with production prompts isolated outside the domain |
| In-memory rendered-message record repository (Impl 015, `rendering/application/`) | Auditability/review must be proven *before* delivery/persistence tech is chosen | Proves append-only records + review + derived eligibility with no DB; mutation isolation + validated reconstitution | A real backend behind the **same port**, plus (later) ref-only rendered-output events and a delivery/UI/API surface |
| Deterministic `InMemoryTestSink` (Impl 016, `delivery/domain/delivery-sink.ts`) | The delivery *boundary contract* must be proven *before* any real channel/provider; tests must be deterministic | Proves eligibility-gated exposure semantics with no external call/nondeterminism; the **eligibility gate** (`displayEligibilityOf`) is the channel-independent guarantee a real sink must also respect | A real channel/provider adapter **behind the same `DeliverySink` interface** and the same eligibility gate |
| In-memory delivery audit repository (Impl 016, `delivery/application/`) | Delivery auditability must be proven *before* a delivery transport/storage is chosen | Proves auditable `DeliveryRecord`s (request + eligibility + outcome) with no DB; mutation isolation + validated reconstitution; success/failure never touch the domain | A real backend behind the **same port**, plus (later) ref-only delivery events and a real channel surface |
| Deterministic `FakeProviderAdapter` (Impl 017, `rendering/application/fake-provider-adapter.ts`) | The provider *seam contract* must be proven *before* any real provider; tests must be deterministic | Proves the draft→validate handoff (and every rejection/failure) with no provider/network/nondeterminism; the **unchanged `validateDraft`** is the provider-independent guarantee a real adapter must also pass | A real provider adapter **behind the same `ProviderAdapter` port** and the same validator, with secrets/network/prompts isolated outside the domain |
| In-memory provider-attempt audit repository (Impl 018, `rendering/application/`) | Provider-attempt auditability must be proven *before* a real provider (and its telemetry/eval temptations) arrives; the audit must observe, never act | Proves safe-summary records (status + reasons, **no raw draft**) with no DB; mutation isolation + validated reconstitution; the audit calls no provider/validator and triggers nothing | A real backend behind the **same port**, plus (later) ref-only provider events; a real provider integration consumes the same audit contract — never by retaining raw drafts as source material |
| Deterministic `FakeProviderClient` + `ProviderSecretRef` (Impl 019, `rendering/`) | The real-provider *boundary contract* (async client, secrets, instructions, failure mapping) must be proven *before* any real SDK/network/secret; tests must be deterministic | Proves the async draft→validate handoff, the credential fast-path, and every operational-failure mapping with no real provider/network/secret; the **unchanged `validateDraft`** + the operational `ProviderSecretRef` are the provider-independent guarantees a real client must respect | A real client implements the same async `ProviderClientBoundary` (secrets/network/prompts isolated there) behind the same validator — never by leaking a raw secret or bypassing the gate |
| Disabled-by-default `ConcreteProviderClient` + neutral serializer/parser/error-mapper (Impl 020, `rendering/application/`) | The first **selected-provider** adapter's vendor-specific serialization/parsing/error-mapping must be proven *before* any live call/SDK/credential; the provider target is chosen (020A) but live integration is deferred; tests must be deterministic and credential-free | Proves provider-specific request/response/error mapping behind the async `ProviderClientBoundary` with **no live call, no SDK, no network, no secret** — disabled by default (safe failure); keeps the vendor **doc-level only** so guards stay intact; the **unchanged `validateDraft`** remains the authority | A real **live** client replaces the disabled shell's transport behind the same `ProviderClientBoundary` and the same validator (credentials/network isolated there) — never by weakening a guard, leaking a secret, or bypassing the gate |
| `StaticProviderCredentialResolver` + injected `LiveCallPolicy` (Impl 021, `rendering/application/`) | The live-call *gate* (opt-in policy + credential resolution) must be proven *before* any real env/secret mechanism exists; tests must be deterministic and credential-free | Proves fail-closed opt-in + credential states (available/missing/invalid) drive safe behavior with **no `process.env`, no real secret, no network** (the credential is a non-secret sentinel; missing/invalid/disabled fail closed before transport) | A **production secret/env resolver** implements the same `ProviderCredentialResolver` port (env/secret-manager isolated there) and a deliberate opt-in supplies an `enabled` `LiveCallPolicy` — never by reading `process.env` in application logic or persisting a secret |
| `LiveProviderHttpTransport` over native `fetch`, injected endpoint (Impl 021, `rendering/application/`) | The native-HTTP *containment* must be proven *before* a real endpoint/SDK is chosen; the network token must live in exactly one auditable place | Proves a real call can be made behind the boundary with **no SDK/dependency** and the network token sealed in **one approved file** (vendor/SDK/env forbidden everywhere); the transport validates/persists nothing and leaks no secret | A real endpoint (and, if ever justified, an SDK) supplied to the same transport seam — the network token stays confined to this one file behind the same surgical guard exception |
| Reprojection harness (Impl 012, `__tests__/reprojection-harness/`) | Reprojection must be proven *before* any production recompute service/scheduler is specified; it crosses repositories/events/modules and no production application layer exists | Keeps reprojection a check-only safety mechanism — recompute-and-report, no mutation — without implying a scheduler/projection-engine/service | A production orchestration/application layer that promotes the harness concepts behind unchanged contracts when a real entrypoint is specified |
| No production orchestration service (test harness is the seam) | No external entrypoint boundary specified | Avoids implying a use-case boundary; keeps invariants inside aggregates | A production application service when a real entrypoint is specified |

[FACT] **`reasoning` already carries `Hypothesis.purposeContextRef`** (a string slot), so a `PurposeVersionRef` flows in with no reasoning refactor — but **reasoning is not yet purpose-version-aware** (it stores the handle opaquely; it does not reinterpret past hypotheses on a purpose change). That deeper integration is a later spec.

---

## 8. Boundary Rules Going Forward

[DECISION] Future work must preserve these (consolidated from the Boundary Map and the domain index):

1. **`observation` remains upstream** — it imports only `shared-kernel`; it never learns about signals-as-meaning, hypotheses, or voice.
2. **`reasoning` may depend on `observation`, not vice versa** — reasoning consumes signals; observation never imports reasoning.
3. **`understanding` consumes reasoning *outcomes*, not raw `Signal`s** — only via the `ReasoningOutcome` adapter; never reads claim confidence or raw evidence.
4. **`decision-support` consumes `reasoning` and `understanding`, not vice versa** — the upstream modules must never import `decision-support`.
5. **`athlete` is the upstream context of meaning** — it imports only `shared-kernel` and must never import `observation`/`reasoning`/`understanding`/`decision-support` (the *given* must not depend on the *inferred*). Purpose reaches downstream only through neutral adapters/inputs, never by `athlete` reaching out.
6. **`Athlete` must not own inferred state or capacity** — those remain projections (defeasible, traceable), never authoritative attributes; the slice holds only the *given* (declared, versioned purpose; athlete-recorded decisions).
7. **A purpose change never rewrites the past** — `PurposeHistory` is append-only; prior reasoning is never edited/auto-falsified; understanding is staled *selectively* through a coordinator, never reset globally, and never mutated by `athlete` directly.
7b. **The athlete owns the decision; Aurora references and learns, never grades (Impl 009)** — `AthleteDecision` is athlete-owned and append-only; `decision-support` records only an `AthleteDecisionRef`; the decision re-enters **only as `Observation`** (then the full ladder), never as Evidence/Understanding directly; **no compliance/obedience scoring**, and the outcome never grades `SupportQuality`.
8. **UI / LLM must not become domain authority** — generated text is a rendering of domain values, never their source of truth.
9. **Persistence must not turn projections into facts** — a stored `CurrentState`/`UnderstandingLevel` must keep its staleness/confidence; never a plain attribute.
10. **A projection is a derived view, never a source of truth (Impl 008)** — it carries explicit freshness + source references; non-current freshness can only *lower* downstream assertiveness; refresh *recomputes* (never edits the old view); a trigger stales *selectively*, never globally. Consumers honor freshness through the owning module's output (e.g. the clamped `safeVoiceCeiling`), not by owning refresh.
11. **No production service may bypass aggregates or gates** — application services *coordinate, never reason*; a voice/level/verdict is produced only inside its owning module. (Boundary Map's named "most dangerous shortcut.")
12. **Persistence preserves the model; it never becomes the model (Impl 010)** — repositories `save`/`findById` only; rehydration goes through a **validating `reconstitute()`** (never a raw field bag); they store **state copies, not live references**; they **create no domain meaning**; the **domain's `toState()` drives the store, never the reverse**; and a repository **never owns** what its aggregate only references (e.g. a `DecisionSupportCase` repo never persists an `AthleteDecision` object).
13. **Event records remember what happened; they never command it (Impl 011)** — a `DomainEventRecord` is **append-only, ref-only, and inert**: it is **not** a command/aggregate/projection/source-truth, payloads carry **refs not copied state**, appending **executes nothing**, and **causation/correlation are lineage/grouping, never an execution chain**. The log **complements, never replaces**, the aggregate repositories; **`event-recording` imports only `shared-kernel`**, **no domain module imports it**, the **catalog stays out of `shared-kernel`**, and there is **no event bus / publish-subscribe / handler / async delivery / event sourcing / serialization tech**.
14. **Reprojection recomputes and verifies; it never mutates, executes, or asserts truth (Impl 012)** — a run is **check-only**: it recomputes derived views *only through the owning module's functions*, **recalculates** freshness (re-deriving only the same-or-more-cautious view — never promotes), reads **event records as candidates/context only** (never replayed/executed; never rebuilds an aggregate from the log), and **reports drift** instead of overwriting. It **mutates no repository**, creates **no** `TerminalOutput`/recommendation/`SupportQuality` rewrite/`Purpose` overwrite/`DomainEventRecord`, and stays a **neutral test-support seam** — **no production `reprojection` module, scheduler, event sourcing, projection repository, or service layer**, and **no domain module imports it**.
15. **Ingestion records source material; it never interprets (Impl 013)** — the Manual Input Adapter lives in `observation/application`; its **only output is an `ObservationSet`** (via `recordObservationSet`, persisted through `ObservationSetRepository`); it preserves **verbatim words / explicit missing data / provenance (`source: "manual"`) / quality**, **rejects** the unrepresentable (saving nothing), and **partially accepts** only faithful entries. It **detects no `Signal`**, **infers no state**, **invents no value**, **mutates no `AthleteDecisionRecord`**, **scores no compliance**, and **imports no downstream module or `event-recording`** — `observation` stays `event-recording`-free, and any `ObservationSetRecorded` is harness-composed, ref-only, and inert. **Manual input is source material, never meaning.**
16. **Rendering expresses the decision; it never makes one (Impl 014)** — the `rendering` module is **downstream presentation**: it reads a read-only projection of a completed `TerminalOutput` and owns **phrasing only**. A **deterministic fake renderer** produces drafts; **every draft passes a mandatory validator** (the validator is the guarantee) that lets voice **match or soften, never escalate**, keeps `Inquiry` a question and `Withholding` a refusal, keeps uncertainty/limitations/freshness/traceability **visible**, and rejects invented facts/citations + unsafe domain language (**safe non-render** on failure). Generated text is **never domain authority** (`≠ Evidence/Observation/Understanding/AthleteDecision`); rendering **selects no voice, creates no `TerminalOutput`/recommendation, mutates no aggregate, writes no event**; it imports only `shared-kernel` + read-only `decision-support` types, **no domain module imports it**, and **no real LLM provider/prompt/UI/API/external call** exists. **Generated text is a presentation artifact, never authority.**
17. **Persistence audits the presentation; review judges display, not reasoning (Impl 015)** — `rendering` keeps an **append-only `RenderedMessageRecord`** (auditable; source-domain-output ref preserved) and an **append-only `RenderReview`** (display-safety only), with **derived** current status + **derived `DisplayEligibility`**, behind a **repository port + in-memory adapter**. **Persisting is auditability, not authority**: a record is never domain truth; approval changes no `VoiceMode`/traceability/freshness/`SupportQuality` and creates no `Recommendation`; rejection invalidates nothing; failed attempts are never display-eligible/approvable; revision/supersession preserve the old record; **display eligibility is not delivery**. **`rendering` imports no `event-recording`**, the **event catalog is not expanded**, the repo is **in-memory** (no production DB), and there is **no delivery/UI/API/provider**. **Persisting or approving rendered text improves auditability and display safety only.**
18. **Delivery exposes the reviewed message; it never makes it true (Impl 016)** — the `delivery` module is **downstream exposure**: it **verifies** display eligibility by calling `rendering`'s **`displayEligibilityOf(record)`** (never re-deriving it), exposes an eligible record **only** to a deterministic **`InMemoryTestSink`** (only `test-sink` supported), records an auditable **`DeliveryRecord`** behind a port + in-memory adapter, and **blocks** every ineligible (not-reviewed/rejected/superseded/failed-render/missing-ref) or unsupported-target request without calling the sink. **Delivery is auditability, not authority**: a `DeliveryRecord` is never source truth (`≠ Evidence/Observation/Understanding/DecisionSupport/AthleteDecision`); **delivery success is not evidence** and **delivery failure is not domain invalidation**; delivery **mutates no rendered record/aggregate**, **emits no event**, and **triggers no reasoning/reprojection/retry**. `delivery` imports **only `shared-kernel` + read-only `rendering`** (no `event-recording`), the **event catalog is not expanded**, **`rendering` does not import `delivery`**, **no upstream module imports `delivery`**, and there is **no real provider/channel / UI / API / scheduler / retry / event bus / production DB**. **The test sink is not a real provider/channel; a delivered message is still a presentation artifact, never authority.**
19. **A provider drafts; the validator decides (Impl 017)** — the `rendering` provider seam replaces **only** the draft-text step: a (fake, deterministic) `ProviderAdapter` returns an **untrusted `ProviderDraft`**, and a `RenderedMessage` exists **only** by passing the **unchanged mandatory `validateDraft`** (the validator, not the provider, is the authority). The provider request is a **constrained projection** of `RenderingRequest` (no raw reasoning / chain-of-thought / mutable handle / override-voice / hide-uncertainty / prompt-injection field — unrepresentable); an **unsafe request is refused before the provider call**; **every** provider failure or unsafe draft **degrades to safe non-rendering** (a validation failure → `provider-output-failed-validation` + the underlying `RenderingFailure[]`). The provider **selects/changes no `VoiceMode`**, **creates no `TerminalOutput`/`Recommendation`/`RenderedMessage`/`RenderedMessageRecord`**, and **persists/reviews/marks-display-eligible/delivers/emits-an-event/mutates nothing**. The seam imports **only** its own `rendering` surfaces + read-only `decision-support` *types*; **no module outside `rendering` imports it**; and there is **no real provider SDK/API/network/`process.env`/prompt-templates-as-code and no `provider`/`llm` top-level module**. **A provider draft is an untrusted presentation draft, never authority; only the validator turns it into a message.**
20. **The audit remembers the attempt, never the draft (Impl 018)** — the `rendering` provider-attempt audit **observes** a `ProviderRenderOutcome` and records an append-only **`ProviderAttemptRecord`** (port + in-memory adapter): a **safe summary** (status `validation-passed`/`validation-failed`/`provider-failed`/`unsafe-request-blocked` + reasons reusing the real `ProviderFailure` + `RenderingFailure` catalogs) with **no raw draft retention** (`rawDraftRetained` literal `false`; reconstitution rejects raw draft/text/content/prompt fields, `rawDraftRetained: true`, unknown payload-bag keys, and status/flag mismatches). It is **observe-only** — it **does not call** the provider / `requestProviderRendering` / `validateDraft` — and **auditability, not authority**: a record is **not** a `ProviderDraft`/source-truth/`Evidence`/`Observation`/`Understanding`/`AthleteDecision`/`DecisionSupport`/`TerminalOutput`/`RenderedMessage`/`RenderedMessageRecord`; it **creates no review/display-eligibility/delivery**, **appends no event**, **triggers no retry/scheduler/reprojection/reasoning**, and **mutates nothing**; a **validation failure is not domain invalidation**, provider success is not recommendation validation, and provider failure does not weaken `SupportQuality`. The audit imports **only** its own `rendering` surfaces + read-only `decision-support` *types*; **no module outside `rendering` imports it**; **no `event-recording` import / event-catalog expansion**; and it is **not model evaluation or telemetry infrastructure**. **No raw draft retention is not no auditability; the safe summary is the audit.**
21. **A real provider changes the draft source, not the authority model (Impl 019)** — the `rendering` real-provider-*ready* path is **additive and async**; the **synchronous Impl 017 seam is untouched** (Option B). `requestRealProviderRendering` **reuses** the unchanged `providerRenderingRequestFrom` guard (unsafe requests refused **before** any client call), a **credential fast-path** (a non-`present` `ProviderSecretRef` fails safely before the call), the deterministic async `ProviderClientBoundary`/`FakeProviderClient`, and the **same mandatory `validateDraft`**; it returns the **existing `ProviderRenderOutcome`**. **Secret refs, never secrets** (status + opaque ref; no raw secret in records/responses/errors; **no `process.env`**; never audited); **structured instruction, not prompts** (derived; no template/arbitrary-prompt/chain-of-thought); **operational failures map down** to the existing `ProviderFailure` (**catalog not expanded**); every failure **degrades to safe non-rendering** with **no automatic retry**. Provider output is **untrusted draft** (only `validateDraft` makes a message); **provider metadata is operational, not evidence**; **provider success validates no recommendation quality** and **failure invalidates no domain output**; the raw-free audit observes the outcome by **explicit composition** (**no automatic persistence**); and there is **no review/display-eligibility/delivery/event/domain mutation**. It imports **only** its own `rendering` surfaces + read-only `decision-support` *types*; **no module outside `rendering` imports it**; and there is **no real provider SDK/API/network/`process.env`/prompt-template and no `provider`/`llm`/`telemetry`/`evaluation` top-level module**. **Real-provider-*ready*, not real-provider-*integrated*; the gate, the audit, and the domain are unchanged.**
22. **A provider is selected in docs, neutral in code; a shell prepares the vendor without becoming it (Impl 020)** — the first **concrete-provider adapter** lives inside `rendering/application` behind the Impl 019 async `ProviderClientBoundary`. The provider target (**OpenAI**) is recorded at the **doc/decision level (020A) only**; production/test code stays **vendor-neutral** (`concrete-provider-*`, `providerKind: "concrete"`) so **no negative-capability guard is weakened and no vendor token (`openai`/`anthropic`) appears in a guarded provider file**. `ConcreteProviderClient` is **disabled by default** (no transport → safe `provider-unavailable`, no I/O — **there is no live-call code path**); a non-`present` credential fails safe before any transport; the only non-default behavior is a **deterministic in-process fixture transport for tests** (never a network call). The **serializer** projects **only** safe constraints (no arbitrary-prompt/chain-of-thought/voice-override/secret field — unrepresentable; **no prompt template / `src/prompts`**); the **parser** returns an **untrusted draft + operational metadata only** (empty/malformed → safe operational failures; **no raw payload retained**; **no `RenderedMessage`**; metadata is operational, never evidence); the **error mapper** maps provider-shaped errors to the existing `ProviderOperationalFailure`, which `toProviderFailure` maps **down** to the existing `ProviderFailure` (**not expanded**; unknown → `provider-unavailable`, no leak). A draft becomes a message **only** via the unchanged `validateDraft`; the raw-free audit observes the outcome by **explicit composition** (no automatic persistence). It imports **only** its own `rendering` surfaces + read-only `decision-support` *types*; **no module outside `rendering` imports it**; there is **no installed SDK/package dependency** (`package.json`/lockfile unchanged), **no network/`process.env`/raw secret**, and **no retry/scheduler/record/review/display/delivery/event/domain side effect**. **A selected-provider *shell* prepares the vendor deterministically; it is not a live integration, and it weakens nothing.**
23. **A live call is operational I/O behind the gate; opt-in is explicit and fail-closed (Impl 021)** — the `rendering` live-provider boundary makes a provider call *able to be real* without changing the authority model. A **`LiveProviderClient`** implements the existing async `ProviderClientBoundary` (a **sibling** of `ConcreteProviderClient`, reusing the unchanged `serializeProviderInstruction`/`parseProviderResponse`/`mapProviderError`); it **never calls `validateDraft`** — validation stays owned by the unchanged `requestRealProviderRendering`. It is **disabled by default** (`LiveCallPolicy.disabled()`), **opt-in is explicit** via the injected policy (never inferred from the environment; no global state), and it **fails closed before any transport call** when the policy is disabled or the credential (from the injected `ProviderCredentialResolver`) is missing/invalid. **Native `fetch` is sealed in exactly one approved file** (`live-provider-http-transport.ts`, behind an injected endpoint, `AbortSignal.timeout`, no retry/persist/validate/secret-in-errors); **no SDK/package dependency** (`package.json`/lockfile unchanged), **no `process.env`/env resolver/real secret** (the credential is a non-secret sentinel, used transiently, never persisted/audited/logged/in errors), and **no default/CI live call or credential**. Failures map **down** onto the existing `ProviderOperationalFailure → ProviderFailure` (**not expanded**; unknown → safe `provider-unavailable`); provider output is **untrusted draft** (only `validateDraft` makes a message); **provider metadata is operational, not evidence**; the raw-free audit observes by **explicit composition** (no automatic persistence); and there is **no review/display-eligibility/delivery/event/domain mutation** and **no retry/scheduler/event bus**. The **network-guard exception is surgical** — one file, network-token only; **vendor / SDK / env tokens stay forbidden everywhere** (Impl 019/020 guards untouched). The synchronous seam, `FakeProviderAdapter`, `FakeProviderClient`, and `ConcreteProviderClient` are **untouched**. It imports **only** its own `rendering` surfaces + read-only `decision-support` *types*; **no module outside `rendering` imports it**. **A live call changes the draft source, never the authority model; it is live-call-*capable*, not a production rollout.**

---

## 9. Next Responsible Missions

[ASSUMPTION] Ranked. Each names why it matters, what it must **not** do, and what it depends on.

0. **✅ Spec 007 — Athlete Purpose Change and Reasoning Reinterpretation — DONE (Impl 007).**
   - *Delivered:* a thin, Purpose-first `athlete` module (declared, versioned, append-only purpose; `PurposeChanged`; `PurposeVersionRef`); purpose flows to `decision-support`/`understanding` via neutral adapters; selective staleness; no reasoning rewrite.
   - *Deferred (carried forward):* reasoning becoming **purpose-version-aware** and the **reinterpretation engine** (status type ships, engine does not); the **full** `athlete` aggregate (state/capacity/constraints/path-memory).

0b. **✅ Spec 008 — Projection Refresh and Staleness Strategy — DONE (Impl 008).**
   - *Delivered:* explicit `ProjectionFreshness` (5 states) + `derivedAt` + source refs on `UnderstandingAssessment`; a pure, selective, conservative `ProjectionRefreshPolicy`; non-current freshness only lowers the ceiling (invalid/unknown → `none` → Withholding); `decision-support` unchanged.
   - *Deferred (carried forward):* a generic projection kernel/engine; `ImpactAssessment` as a second projection; event-driven/persisted refresh; `Inquiry`-on-unknown (currently Withholding).

0c. **✅ Spec 009 — AthleteDecision Feedback Loop — DONE (Impl 009).**
   - *Delivered:* athlete-owned, append-only `AthleteDecision` in `athlete` (choice/rationale/context/source; amend-supersede); `DecisionSupportCase` references only an `AthleteDecisionRef`; re-entry as `SubjectiveObservation` via a neutral adapter; no compliance/obedience scoring; outcome never grades `SupportQuality`. The `AthleteDecisionRef` placeholder is retired.
   - *Deferred (carried forward):* a full `DecisionOutcome` object; a decision-pattern→hypothesis engine; a feedback/interaction bounded context if interaction history grows.

0d. **✅ Implementation Architecture — Persistence and Event Surface — DONE (paper).** Defines persistence/event-surface boundaries (ports, append-only ref-only events, projections-as-caches, traceability handles); chooses no technology.

0e. **✅ Spec 010 — Persistence Ports and In-Memory Repositories — DONE (Impl 010).**
   - *Delivered:* validated `toState()`/`reconstitute()` + module-owned ports + in-memory adapters for the six boundaries; round-trip preserves invariants/traceability/freshness/append-only history; mutation isolation; invalid-state rejection; no technology chosen.
   - *Deferred (carried forward):* domain **event/outcome records** + traceability envelope (Spec 011); reprojection harness; production DB/event-bus/persistence backend.

0f. **✅ Spec 011 — Domain Event/Outcome Records and Traceability Envelope — DONE (Impl 011).**
   - *Delivered:* a dependency-neutral `event-recording` module — one append-only, ref-only `DomainEventRecord` (occurrence/outcome) over a closed 26-type catalog, a reusable `TraceabilityEnvelope`, an append-only in-memory log + repository port + adapter; catalog-driven validation on construct *and* reconstitute; causation/correlation as lineage/grouping; imports only `shared-kernel`; no domain module imports it.
   - *Deferred (carried forward):* the **reprojection harness** (Spec 012); a **projection repository**; an **event bus / production event store / serialization format**; whether **event sourcing** is ever appropriate.

0g. **✅ Spec 012 — Reprojection Harness — DONE (Impl 012).**
   - *Delivered:* a neutral, check-only recompute-and-compare harness (`__tests__/reprojection-harness/`) that recomputes `UnderstandingAssessment` via the owning module, recalculates freshness, detects candidates from event records (context only), and reports drift/findings — mutating nothing, rebuilding nothing from the log, promoting no freshness, creating no output. Reserved modes throw.
   - *Deferred (carried forward):* a **production recompute service / orchestration layer**, a **scheduler / event-driven refresh**, a **projection repository**, `ImpactAssessment` as a second projection, and any `refresh-derived`/`mark-stale` write path.

0h. **✅ Spec 013 — Manual Input Adapter — DONE (Impl 013).**
   - *Delivered:* an `observation`-owned Manual Input Adapter (`ingestManualInput`) that records manual input faithfully as an `ObservationSet` (verbatim words, explicit missing data, provenance `source: "manual"`, quality), persists via `ObservationSetRepository`, and returns accept/partial/reject — detecting no `Signal`, inferring nothing, mutating no athlete record, importing no downstream module or `event-recording`.
   - *Deferred (carried forward):* a **UI/API entrypoint** that collects/submits input; **auth/source identity**; **external (FIT/wearable) ingestion**; richer measured-value/unit handling; duplicate detection.

0i. **✅ Spec 014 — LLM Rendering Boundary — DONE (Impl 014).**
   - *Delivered:* a deterministic `rendering` module — `RenderableDomainOutput` (read-only projection), a provider-free fake renderer, and a **mandatory validator** that preserves voice/uncertainty/limitations/freshness/traceability/agency and rejects escalation/invention/hidden-uncertainty (safe non-render). Rendering mutates/persists/emits nothing; imports only `shared-kernel` + read-only `decision-support` types.
   - *Deferred (carried forward):* a **real LLM provider** (behind the same validator) + production prompts; **rendered-message persistence** + **rendered-output event records**; UI/API/external delivery; a human approval workflow.

0j. **✅ Spec 015 — Rendered-Message Review / Persistence — DONE (Impl 015).**
   - *Delivered:* inside `rendering`, an append-only `RenderedMessageRecord` + append-only `RenderReview` (display-safety) + derived `DisplayEligibility`, behind a repository port + in-memory adapter — auditability not authority; approval/rejection touch no domain; failed attempts never display-eligible; no events; no DB/delivery/UI/API.
   - *Deferred (carried forward):* ref-only **rendered-output events**; a **delivery / UI / API** surface for approved messages; a **real LLM provider** behind the validator; a production persistence backend.

0k. **✅ Spec 016 — Delivery Boundary — DONE (Impl 016).**
   - *Delivered:* a new downstream `delivery` module that exposes a *display-eligible* `RenderedMessageRecord` to a deterministic test-only sink and records an auditable `DeliveryRecord` (port + in-memory adapter); verifies (never re-derives) eligibility via `displayEligibilityOf`; blocks not-reviewed/rejected/superseded/failed-render/missing-ref/unsupported-target without calling the sink; success/failure never touch the domain; imports only `shared-kernel` + read-only `rendering`; no events, real channel/provider, UI/API, scheduler/retry, event bus, or DB.
   - *Deferred (carried forward):* a **real provider/channel** behind the `DeliverySink` interface; a **delivery event surface** (ref-only `DeliveryAttempted`/`DeliveryOutcomeRecorded`); a **delivery UI/API**; retry/cancellation lifecycles + the `accepted-for-delivery` two-phase flow; a production persistence backend.

0l. **✅ Spec 017 — Provider Adapter Boundary — DONE (Impl 017).**
   - *Delivered:* a provider adapter seam inside `rendering` — a constrained `ProviderRenderingRequest` (+ guard), an untrusted `ProviderDraft`, a closed `ProviderFailure` catalog, a `ProviderAdapter` port, a deterministic `FakeProviderAdapter`, and a `requestProviderRendering` service that runs the **unchanged mandatory `validateDraft`**; the provider replaces only the draft-text step; unsafe requests refused before the call; every failure degrades to safe non-rendering; no real SDK/API/network/prompt, no `provider`/`llm` module, no persistence/review/eligibility/delivery/event side effect.
   - *Deferred (carried forward):* a **real provider integration** behind the same `ProviderAdapter` port (secrets/network/prompts isolated); **provider-attempt persistence / provider events**; production prompts + safety evaluation beyond deterministic validation.

0m. **✅ Spec 018 — Provider Attempt Audit Boundary — DONE (Impl 018).**
   - *Delivered:* a provider-attempt audit inside `rendering` — an append-only `ProviderAttemptRecord` (port + in-memory adapter) built by observing a `ProviderRenderOutcome`; safe summary (status + real-catalog reasons), **no raw draft retention**; observe-only (calls no provider/validator); creates no record/review/display/delivery/event; a validation failure is not domain invalidation; reasons reuse the real `ProviderFailure` + `RenderingFailure` catalogs; no provider event / event-catalog expansion / real SDK / telemetry / model evaluation.
   - *Deferred (carried forward):* a **real provider integration** behind the same `ProviderAdapter` port (secrets/network/prompts isolated); **provider/delivery event records**; provider telemetry / model evaluation; a future raw-retention/redaction policy (only if separately specified).

0n. **✅ Spec 019 — Real Provider Integration Boundary — DONE (Impl 019, real-provider-*ready*).**
   - *Delivered:* an additive async real-provider-ready boundary inside `rendering` — async `ProviderClientBoundary` + deterministic `FakeProviderClient`, operational `ProviderSecretRef`, structured `ProviderInstruction`, `ProviderOperationalFailure` mapped down to the existing `ProviderFailure` (not expanded), and `requestRealProviderRendering` reusing the unchanged guard + `validateDraft` and returning the existing `ProviderRenderOutcome`. The sync Impl 017 seam is untouched; secrets/metadata stay operational; the audit stays raw-free (explicit composition); every failure degrades to safe non-rendering with no automatic retry; no real SDK/network/secret/prompt, no new module, no downstream side effect.
   - *Deferred (carried forward):* the **real provider adapter implementation** (a real client behind the async `ProviderClientBoundary` — secrets/network/prompts isolated there); **provider/delivery event records**; provider telemetry / model evaluation; a future raw-retention/redaction policy.

0o. **✅ Spec 020 — Real Provider Adapter Implementation Boundary — DONE (Impl 020, selected-provider *shell*).**
   - *Delivered:* the first **concrete-provider adapter shell** inside `rendering/application` behind the Impl 019 async `ProviderClientBoundary` — a **disabled-by-default `ConcreteProviderClient`** + pure `serializeProviderInstruction` / `parseProviderResponse` / `mapProviderError`. The provider target (**OpenAI**) is selected at the **doc/decision level (020A) only**; code stays vendor-neutral (`concrete-provider-*`), so **no guard was weakened and no vendor token leaks into a guarded file**. Draft → unchanged `validateDraft`; operational failures map **down** to the existing `ProviderFailure` (not expanded); raw-free audit by explicit composition. **No SDK/package change, no network, no live call, no `process.env`, no raw secret, no prompt template, no retry/scheduler, no record/review/display/delivery/event/domain side effect.**
   - *Deferred (carried forward):* **live provider call enablement** (the actual network/credential opt-in, still behind the validator and raw-free audit); **provider/delivery event records**; provider telemetry / model evaluation; a future raw-retention/redaction policy.

0p. **✅ Spec 021 — Live Provider Call Enablement Boundary — DONE (Impl 021, opt-in, fail-closed).**
   - *Delivered:* an opt-in live-provider boundary inside `rendering/application` — a `LiveProviderClient` (implements the async `ProviderClientBoundary`; sibling of `ConcreteProviderClient`, reusing the unchanged serializer/parser/error-mapper), an injected `LiveCallPolicy` (disabled by default; explicit opt-in; no env inference), an injected `ProviderCredentialResolver` + deterministic `StaticProviderCredentialResolver` (no env, non-secret sentinel), and a single `LiveProviderHttpTransport` (native `fetch` behind an injected endpoint — the only network-token file). Fails closed before transport when disabled/missing/invalid; never calls `validateDraft` (validation stays with `requestRealProviderRendering`); failures map down onto the existing `ProviderOperationalFailure → ProviderFailure` (not expanded); audit raw-free by explicit composition. **No SDK/dependency/lockfile change, no `process.env`, no default/CI live call or credential, no record/review/display/delivery/event/domain side effect, no retry/scheduler.** The network-guard exception is surgical (one approved file); the sync seam, fakes, and concrete shell are untouched.
   - *Deferred (carried forward):* a **production secret/environment resolver** (a real `ProviderCredentialResolver` reading env/secret-manager, isolated) + a deliberate production opt-in; a **real endpoint/SDK** rollout; **provider/delivery event records**; provider telemetry / model evaluation; a future raw-retention/redaction policy.

1. **Spec 022 — Environment Secret Resolver Boundary *or* Provider/Delivery Event Surface** *(recommended next — choose explicitly)*
   - *Why:* the live boundary is now **capable but disabled** with **no env/secret resolver**. The remaining edges are (a) an **environment/secret resolver boundary** — the first slice that **actually** resolves a real credential (env var / secret manager) behind the injected `ProviderCredentialResolver` port, isolated to one approved file like the transport, still credential-free in CI/default tests, or (b) recording **provider/delivery occurrences as ref-only events** (an event surface). Each is a genuine "real outside world" boundary and should be opened deliberately, one at a time.
   - *Must not:* read `process.env` outside the one approved resolver file, or let a real secret enter domain/audit/records/errors/logs; enable live calls by default or require credentials in CI/local tests; weaken a guard or leak a vendor token; let a live client skip the validator; let a provider/delivery event become a command/retry trigger; let a drafted/delivered artifact become source truth; introduce UI/API/channel without a strict boundary contract.
   - *Depends on:* `rendering` (validator + provider seam + attempt audit + real-provider-ready client boundary + concrete-provider shell + opt-in live boundary, done) + `delivery` (eligibility-gated exposure + audit, done); a deliberate choice of which edge to open first.

2. **Reasoning Purpose-Version Awareness & Reinterpretation Engine** *(the deferred half of Spec 007)*
   - *Why:* Impl 007 ships `PurposeVersionRef` and the reinterpretation *status type* but not the engine; this makes reasoning version-aware and produces real reinterpretation verdicts on a purpose change. It can reuse the Impl 008 freshness/refresh machinery (a purpose change is a `RefreshTrigger`).
   - *Must not:* rewrite or auto-falsify prior hypotheses — reinterpretation is a new, traceable artifact.
   - *Depends on:* `athlete` (Purpose, done) + `reasoning` (done) + projection freshness (done).

3. **Production Reprojection / Refresh layer** *(the deferred half of Spec 012)*
   - *Why:* Impl 012 proved reprojection as a check-only harness; a production recompute *service* (and, later, a scheduler / event-driven refresh and a projection repository) would promote those concepts behind unchanged contracts once a real entrypoint exists.
   - *Must not:* become event sourcing; introduce a write path that bypasses the understanding transition policy; promote freshness or strengthen voice.
   - *Depends on:* Specs 010–012; a real production orchestration boundary.

[DECISION] **Recommended next mission: Spec 022 — Environment Secret Resolver Boundary *or* Provider/Delivery Event Surface (choose explicitly).** With *data in* (Impl 013), *output out* (Impl 014), an **auditable record/review** of rendered messages (Impl 015), an **eligibility-gated delivery boundary** to a deterministic test sink (Impl 016), a **provider adapter seam** proven against a deterministic fake (Impl 017), a **provider-attempt audit** (safe summaries, no raw draft) (Impl 018), a **real-provider-*ready* async client boundary** (Impl 019), a **selected-provider adapter shell** (Impl 020), and now an **opt-in live-provider boundary** (Impl 021 — fail-closed, native `fetch` in one approved file, injected credential resolver, no env/SDK/default-live-call) all proven in-memory, the remaining edges touch the **real outside world**: (a) an **environment/secret resolver boundary** — the first slice that *actually* resolves a real credential behind the injected `ProviderCredentialResolver` port (env/secret-manager isolated to one approved file, still credential-free in CI), (b) recording **provider/delivery occurrences as ref-only events**, and, further out, a **production live-call rollout** (a real endpoint + a deliberate opt-in) and a **real channel** behind the `DeliverySink`. Each is a deliberate boundary to open one at a time — a resolver must never read `process.env` outside its one approved file or leak a secret into domain/audit/records/errors, a live client must never skip the validator, live calls must never be on by default or require CI credentials, a guard must never be weakened to admit a vendor token, an event must never become a command/retry trigger, delivery must never bypass display eligibility, and no drafted/delivered artifact may become source truth. The discipline that produced restraint by construction must hold precisely when a real mouth, a real secret, and a real channel become tempting.

---

## 10. Final Assessment

[ASSUMPTION] **What has Aurora proven?**
That the reasoning core can run **end-to-end with restraint**: raw observations become signals without becoming meaning, signals become evidence only inside falsifiable hypotheses, tested outcomes earn dimension-specific understanding without repetition or confidence leaking in, and a gated decision-support case turns all of it into a *modest, traceable, agency-preserving* `Reflection` — refusing to recommend even when the chain is complete and the gates are clean. The dangerous collapses (data→meaning, inference→attribute, claim-strength→voice) are unrepresentable, defended by negative-capability and boundary tests.

[ASSUMPTION] **What has Aurora not proven yet?**
Anything involving **a real provider/channel/surface, transport, durable storage, and production recomputation**: a **real LLM provider/SDK** + production prompts + a real secret/network mechanism + a **live provider call** (a deterministic rendering boundary, an auditable record/review, a provider adapter seam against a deterministic fake, a provider-attempt audit, a real-provider-*ready* async client boundary, a vendor-neutral selected-provider adapter shell — disabled by default — **and** an opt-in live-provider boundary — fail-closed, native `fetch` in one approved file — exist, but no **production** live call / real SDK/env-secret-mechanism / production prompts — it is *ready, shelled, and live-call-capable*, not *deployed*), a **real delivery channel/provider** and a **UI / API** (an eligibility-gated delivery boundary to a deterministic *test sink* exists, but no real channel), **external (FIT/wearable) ingestion**, **provider-attempt / rendered-output / delivery event records**, **provider telemetry / model evaluation**, a **scheduler/retry layer**, a **production** persistence backend / event store, a production orchestration entrypoint, the **full** `athlete` model (state/capacity/constraints/path-memory), risk from real data, reasoning that is **purpose-version-aware** with a reinterpretation engine, a **generic projection engine** (`ImpactAssessment`, event-driven/persisted refresh), a **full `DecisionOutcome` / decision-pattern engine**, and a **production reprojection service / scheduler / projection repository**. Purpose **is** a real, versioned upstream source (Impl 007), **projection freshness is explicit** (Impl 008), the **decision feedback loop is built** (Impl 009), **aggregates now round-trip through validated ports + in-memory repositories** (Impl 010), **occurrences are now recorded as an append-only, ref-only event/outcome log** (Impl 011), **derived state can now be recomputed and verified by a neutral check-only harness** (Impl 012), **real manual data now enters as faithful `ObservationSet`s through an observation-owned adapter** (Impl 013), **domain-approved terminal outputs now render to human-facing text through a deterministic, validated boundary** (Impl 014), **rendered messages are now conserved and reviewed as auditable presentation artifacts** (Impl 015), **display-eligible messages are now exposed to a deterministic test sink + audit records through a downstream delivery boundary** (Impl 016), **a provider may now draft rendered text through a fake/test-only seam behind the unchanged mandatory validator** (Impl 017), **provider attempts are now audited as safe-summary records with no raw draft retention** (Impl 018), **a real provider is now pluggable behind an additive async client boundary (fake in-process client, operational secret refs, structured instructions, failure mapping) that still ends at the unchanged validator** (Impl 019), and **the first selected-provider adapter shell now prepares a chosen vendor (OpenAI, doc-level) in vendor-neutral code — a disabled-by-default client + deterministic serializer/parser/error-mapper — behind the same validator with no live call/SDK/secret/prompt** (Impl 020), and **the first opt-in live-provider boundary now lets a real call be made *only when explicitly enabled* (fail-closed `LiveCallPolicy`, injected credential resolver, native `fetch` sealed in one approved file) behind the same validator — with no SDK/dependency, no env read, and no default/CI live call** (Impl 021) — all with **no technology installed and no guard weakened**. The linear path is proven, **three domain loops are exercised** (purpose-change staleness, projection freshness/refresh, decision feedback — all no-rewrite), **persistence is proven safe in-memory**, **the event surface is realized as inert, ref-only records**, **reprojection is proven to recompute-and-verify without mutating/executing/asserting truth**, **the first real ingress records source material without interpreting it**, **the first real egress expresses the decision without becoming authority**, **the output-out cycle is now auditable without the record becoming authority**, **the exposure edge delivers only display-eligible messages without delivery becoming evidence, authority, or a real channel**, **the provider seam lets a draft source feed the unchanged validator without the provider becoming authority, bypassing validation, or touching domain/delivery state**, **the provider-attempt audit remembers what the seam did without retaining a raw draft or letting an attempt become source truth, evidence, or reasoning**, **the real-provider-ready boundary makes a real provider pluggable without secrets, network, prompts, responses, retries, or metadata becoming authority, evidence, persistence, or a downstream side effect**, **the selected-provider adapter shell prepares a chosen vendor deterministically without a live call, an SDK, a secret, a prompt template, a vendor token in guarded code, or any downstream side effect**, and **the opt-in live-provider boundary makes a real call possible only when explicitly enabled — fail-closed, native `fetch` sealed in one approved file, credentials injected (never env-read, never persisted) — without an SDK, a default/CI live call, or any downstream/domain side effect**; the remaining work (a production secret/environment resolver and live-call rollout, a real channel surface, a provider/delivery event surface, a production transport/storage backend, deep purpose reinterpretation, and learning *over time*) is specified or scoped but not yet built.

[ASSUMPTION] **What is the most dangerous next shortcut?**
**Resolving a *real secret*, attaching a *real endpoint*, or flipping the live boundary on in a way that reads `process.env` outside the one approved resolver file, leaks a secret into the domain/audit/records/errors, turns live calls on by default or requires CI credentials, weakens a guard to admit a vendor token/SDK, bypasses the validator, or lets the audit start retaining raw drafts — or a real delivery channel / UI-API surface that bypasses the display-eligibility gate (or treats a drafted/rendered/delivered message as truth).** The rendering *boundary* is proven safe with a deterministic fake renderer + a mandatory validator (Impl 014), the delivery *boundary* with a deterministic test sink behind `displayEligibilityOf` (Impl 016), the provider *seam* with a deterministic fake provider behind the **unchanged** `validateDraft` (Impl 017), the provider-attempt *audit* with safe summaries and **no raw draft retention** (Impl 018), the real-provider *boundary* is now **ready** with an async client + operational secret refs behind the same validator (Impl 019), the first selected-provider adapter *shell* is in place but **disabled by default** with the vendor kept doc-level (Impl 020), and the opt-in live boundary is now **capable but fail-closed** — native `fetch` sealed in one approved file, credentials injected, no env read, no default/CI live call (Impl 021); the danger shifts to the moment a **real secret**, a **real endpoint**, a **real** channel, or a demo-able surface is *actually* attached — letting a resolver read `process.env` outside its one approved file or leak a secret into a record/error/audit, letting a live client skip the validator or be trusted as source truth; letting the audit retain raw drafts "to debug" (re-creating a path for untrusted content to become source material); letting delivery skip eligibility; deriving voice from convenience instead of the gates; or re-ingesting a drafted/rendered/delivered artifact as source truth would reintroduce the exact collapses the core was built to prevent, and it would not look wrong from the inside. The discipline must hold precisely when a real mouth and a real channel become tempting: **a real client goes behind the same async `ProviderClientBoundary` and the same validator (the boundary is already that seam); secrets stay operational refs outside the domain and out of the audit; prompts stay structured/derived; the audit stays raw-free (safe summaries only); a real channel goes behind the same eligibility gate; a drafted/rendered/delivered message is never source truth; delivery success is never evidence; the provider/surface never selects voice.**

---

## Validation

[FACT] Run at the time of writing, with no implementation modified:
- `npm run typecheck` (`tsc --noEmit`) — **clean**.
- `npm test` (`node --test "src/**/*.test.ts"`) — **514 / 514 pass**, 0 fail (was 145 at the 006 baseline; +30 for Impl 007, +21 for Impl 008, +16 for Impl 009, +23 for Impl 010, +40 for Impl 011, +20 for Impl 012, +22 for Impl 013, +32 for Impl 014, +25 for Impl 015, +23 for Impl 016, +24 for Impl 017, +20 for Impl 018, +21 for Impl 019, +31 for Impl 020, +21 for Impl 021), including all module/negative/boundary tests, the Implementation 006 end-to-end Reflection proof, and the Impl 007/008/009/010/011/012/013/014/015/016/017/018/019/020/021 purpose + projection-freshness + decision-feedback + persistence-round-trip + event-record + reprojection-harness + manual-input + rendering + rendered-message-record/review + delivery + provider-seam + provider-attempt-audit + real-provider-ready + concrete-provider-shell + opt-in-live-provider tests.

---

## Success Criterion

> **"What is closed, what is still deliberately absent, and what must future work not collapse?"**

[ASSUMPTION] Answerable from this page: **closed** — the five-stage reasoning core (§2–3) with sixty-three structural guarantees (§4), an end-to-end Reflection proof (§5), a **Purpose `athlete`** module (Impl 007), **explicit projection freshness** on `UnderstandingAssessment` (Impl 008), the **AthleteDecision feedback loop** (Impl 009), **validated persistence ports + in-memory repositories** (Impl 010) so every aggregate round-trips without corruption, a **dependency-neutral append-only, ref-only event/outcome record log** (Impl 011) that records *what happened*, a **neutral check-only reprojection harness** (Impl 012) that recomputes-and-verifies derived views without mutating, executing, or asserting truth, a **real `observation`-owned Manual Input Adapter** (Impl 013) that records manual data faithfully as `ObservationSet`s, a **deterministic, validated `rendering` boundary** (Impl 014) that expresses a domain-approved terminal output as human-facing text without becoming authority, an **auditable rendered-message record + display-safety review** (Impl 015) that conserves and reviews presentation artifacts without making them domain truth, a **downstream `delivery` boundary** (Impl 016) that exposes only display-eligible messages to a deterministic test sink and records auditable attempts without delivery becoming evidence or authority, a **provider adapter seam** (Impl 017) that lets a deterministic fake provider draft rendered text behind the unchanged mandatory validator without the provider becoming authority or bypassing validation, a **provider-attempt audit** (Impl 018) that records what the seam did as safe-summary records with no raw draft retention, a **real-provider-*ready* async client boundary** (Impl 019) that makes a real provider pluggable (operational secret refs, structured instructions, failure mapping) behind the unchanged validator without touching a real provider, a **selected-provider adapter shell** (Impl 020) — OpenAI chosen doc-level, code vendor-neutral, a disabled-by-default client + deterministic serializer/parser/error-mapper — that prepares the vendor without a live call, SDK, secret, prompt, guard weakening, or any downstream side effect, and an **opt-in live-provider boundary** (Impl 021) — fail-closed `LiveCallPolicy`, injected credential resolver, native `fetch` sealed in one approved transport file — that can make a *real* call only when explicitly enabled, behind the unchanged validator, with no SDK/dependency, no env read, no default/CI live call, and no downstream side effect; **deliberately absent** — a **real LLM provider/SDK** + production prompts + a real secret/network mechanism, a **real delivery channel/provider**, a **UI / API**, external (FIT/wearable) ingestion, **provider-attempt / rendered-output / delivery event records**, **provider telemetry / model evaluation**, a **scheduler/retry layer**, a **production** persistence/event-store backend (DB/ORM/schema/migrations/serialization), a production service / reprojection service / projection repository, the **full** `athlete` model (state/capacity/constraints/path-memory), a reinterpretation engine, a **generic projection engine**/`ImpactAssessment`, a **full `DecisionOutcome`/pattern engine**, and **event sourcing**, each a placeholder or intentional gap (§6–7); **must not collapse** — observation into meaning, inference into attribute, claim-strength into voice, the upstream→downstream dependency direction, **purpose's past into its present** (declared ≠ inferred; append-only; no rewrite), **a projection into a source of truth** (freshness explicit; non-current only constrains; refresh recomputes), **the athlete's decision into Aurora's verdict** (referenced not owned; Observation-only re-entry; no obedience/outcome grading), **the persistence shape into the domain** (validated reconstitution; state copies not live refs; the store preserves the model, never becomes it), **an event record into a command, copy, projection, or source truth** (append-only; ref-only; inert; complements repositories, never replaces them), **reprojection into event sourcing, a write path, or a way to make a projection true** (check-only; recompute-and-report; mutates nothing; rebuilds nothing from the log), **manual input into meaning** (faithful `ObservationSet`; verbatim words; explicit missing data; rejects the unrepresentable; detects no signal; infers nothing), **generated text into domain authority** (rendering is downstream presentation; voice never escalates; uncertainty/limitations stay visible; mutates/emits nothing; never re-ingested as truth), **a persisted/approved rendering into domain truth** (records audit, review judges display-safety; approval strengthens nothing; rejection invalidates nothing; display eligibility is not delivery), **delivery into authority, evidence, or reasoning** (downstream exposure; verifies not re-derives eligibility; blocks the ineligible; success ≠ evidence; failure ≠ domain invalidation; test sink ≠ real channel), **a provider draft into authority** (untrusted draft; only `validateDraft` makes a message; provider selects no voice, creates no terminal-output/record, persists/delivers/emits/mutates nothing; failures degrade to safe non-rendering; fake ≠ real integration), **a provider attempt into source truth, evidence, or reasoning** (audit observes not calls; safe summary, no raw draft retained; not `Evidence`/`Observation`/`RenderedMessage`/record; creates no review/display/delivery/event; validation failure ≠ domain invalidation; audit ≠ model evaluation/telemetry), **a real provider into the authority model** (real-provider-*ready* changes the draft source only; sync seam untouched; secret refs ≠ secrets; structured instruction ≠ prompt; operational failure maps down without expanding `ProviderFailure`; metadata ≠ evidence; no automatic retry/persistence/side effect; only `validateDraft` makes a message), **a selected provider into vendor code or a live call** (provider chosen doc-level only; code vendor-neutral so guards stay intact; the concrete client is disabled by default with no live-call path; serializer ≠ prompt; parser output ≠ message and retains no raw payload; error mapper maps down without expanding the catalog; no SDK dependency; a draft becomes a message only via `validateDraft`), and **a live call into the authority model or an always-on/secret-leaking call** (live calls disabled by default; opt-in explicit and fail-closed before transport; native `fetch` sealed in one approved file; no SDK/dependency, no `process.env`/env resolver, no real secret in domain/audit/errors; credentials injected and never persisted; failures map down without expanding the catalog; metadata ≠ evidence; the live client never calls `validateDraft` — validation stays with `requestRealProviderRendering`; no default/CI live call; no downstream/domain side effect) (§4, §8). The core is complete and restrained — purpose real, freshness explicit, the decision loop closed, persistence proven safe in-memory, occurrences recorded as an inert ref-only log, reprojection proven to recompute-and-verify without mutation, real data entering as source material without interpretation, real output expressed without becoming authority, the output-out cycle made auditable without the record becoming authority, the exposure edge delivering only display-eligible messages without becoming evidence or a real channel, the provider seam drafting behind the unchanged validator without becoming authority, the provider-attempt audit remembering the attempt without retaining the draft, and a real provider made pluggable without changing the authority model; what remains is a real provider adapter implementation, a real channel surface, a provider/delivery event surface, a production transport/storage backend, the deeper loops, and learning over time — to be added without eroding any of the above.

---

*This is the first Review / Consolidation paper after the reasoning core was completed in code. It summarizes Implementations 001–006 and is **updated for Implementation 007 (Purpose-first `athlete`), 008 (projection freshness), 009 (AthleteDecision feedback), 010 (persistence ports + in-memory repositories), 011 (domain event/outcome records + traceability envelope), 012 (check-only reprojection harness), 013 (manual input adapter), 014 (deterministic rendering boundary), 015 (rendered-message record/review persistence), 016 (delivery boundary), 017 (provider adapter seam), 018 (provider-attempt audit), 019 (real-provider-ready boundary), 020 (concrete-provider adapter shell), and 021 (opt-in live-provider boundary)**; it makes no new domain or architectural decisions and modifies no module.*

*Inputs: [Foundation Index](../README.md) · [Domain Modeling Index](../domain-modeling/README.md) · [Technical Boundary Map](./TECHNICAL_BOUNDARY_MAP.md) · [System Map](../diagrams/SYSTEM_MAP.md) · [Spec 001](../specs/001-observation-set-intake.md) · [Spec 002](../specs/002-signal-detection.md) · [Spec 003](../specs/003-hypothesis-lifecycle.md) · [Spec 004](../specs/004-understanding-update.md) · [Spec 005](../specs/005-decision-support-voice.md) · [Spec 006](../specs/006-end-to-end-responsible-reflection.md)*
